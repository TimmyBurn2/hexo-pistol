# Decisions

Append-only. One line per non-obvious design choice, in the form

    D-n: choice — reason — what flips it

Lines are never edited or deleted. A decision that changes is superseded by a
new line that says which one it replaces. Silent architecture drift is a breach
(CLAUDE.md rule 10); if the code and a line here disagree, one of them is wrong
and it is usually the code.

---

D-1: Crate cut = CLAUDE.md map verbatim, all 8 crates at workspace init, solver/arena/api as doc-only stubs in Stage 0 — churn-free dependency graph from day 1, stubs pin the seams at zero cost — flips if workspace overhead measurably hurts CI (it won't).

D-2: Engine trait is synchronous, no stop verb in v0 (new_game / set_position / go(Budget) -> SearchOutcome) — at <=0.5 s request/response a stop verb buys nothing and forces async plumbing through the one seam the future API wraps; stop/ponder arrive with Lazy SMP (Stage 4) as an additive trait extension + protocol verb — flips when the API spec requires cancellation.

D-3: Score = Eval(i32) | MateIn(u16) | MatedIn(u16), distance in TURNS; internally MATE = 30_000, MateIn(t) = 30_000 - t, eval clamped to +/-16_000; MateIn(1) means the side to move completes >=6 with a stone placed this turn (first or second stone, same turn count per rule 4); a turn cap scores static eval at the horizon, never 0/draw (rule 6) — flips if solver integration needs proof/disproof score kinds (additive enum variants).

D-4: Budget is the closed enum DepthTurns(u32) | Nodes(u64) | MovetimeMs(u64); absent budget = BudgetMissing; MovetimeMs under mode=instrument = InstrumentBudgetUnsupported — all three budgets exist in play mode, but determinism/strength claims only ever come from depth or nodes — flips never (the schema shape is the decision; values are operator config).

D-5: Line protocol v0 mirrors the Engine trait 1:1; stone token "q,r" signed decimal, pair token "q,r/q,r" canonical lexicographic by (q, then r); verbs pistol/newgame/position/go/quit; every malformed or illegal input yields one line "error <NamedError>: <why>" and the engine stays alive — this protocol IS the future API's semantic contract (Hard Rule 11) — flips if the API spec demands a different position encoding (the move-list form stays canonical regardless).

D-6: The move list is the canonical position encoding (unbounded board, no FEN analog); position set exists for fixtures and carries tomove + phase, with the phase-1 stone identified as the last listed stone of the mover; a set position already containing a >=6 run = IllegalPosition; a pair is legal iff SOME ordering of its two placements is legal under rule 5; movegen and perft count UNORDERED pairs — flips if a fixture format needs richer state (extend set, never relax validation).

D-7: Determinism design: no randomized-state maps on any choice path (fixed-seed hasher or sorted structures), stable sorts with lexicographic (q, then r) final tie-break, deterministic TT replacement (depth-preferred + generation counter, never wall time), integer-only eval, instrument mode = 1 thread enforced by config validation; CI gate = two-process CLI runs over sha-pinned fixtures x budgets {depth_turns 4, nodes 200000}, diffing bestmove + nodes + full pv, any diff = fail; a same-process double-run unit test additionally catches state bleed across newgame — Hard Rule 4 made executable — flips never.

D-8: Zobrist keys are 128-bit, computed as a pure function of (q, r, color, FIXED_SEED) via two independent SplitMix64 streams (caching optional, never value-affecting), XORed with side-to-move and intra-turn phase keys; the search TT indexes from low bits and stores the high 64 bits as verification (~24 B packed entries, 4-entry buckets, deterministic replacement); the solver TT (Stage 1+) stores the full 128 bits plus GHI history hooks; coordinates are i16 with loud overflow asserts — deliberate narrowing of the report's blanket 128-bit line: 64 verification bits are ample for heuristic search, full 128 is reserved for proof soundness where the report's argument actually applies — flips on any observed TT-verification collision in the determinism fixtures, or if solver and search ever share a TT (they won't).

D-9: A turn is represented internally as two sequential same-side plies with the intra-turn phase bit in state and zobrist key; all external accounting (depth, mate distance, perft) is in TURNS over unordered pairs; win check runs after each ply and a phase-0 win ends the turn — the alpha-beta cutoff between the two plies is the main pruning lever (report Section A) and rule 4 falls out structurally — flips never at this design point.

D-10: One EngineError enum in pistol-engine wrapping crate-local errors: Config{key, why}, IllegalMove{turn, why}, IllegalPosition{why}, Protocol{line, why}, BudgetMissing, InstrumentBudgetUnsupported, InternalInvariant{what}; validation = serde deny_unknown_fields on every struct PLUS a validate() pass for cross-field constraints, each failure a named Config error carrying the exact editable key — fail loud (Hard Rule 3) — flips never.

D-11: Eval v0 = handcrafted, integer-only, incremental: for each of the 3 axes every length-6 window through a cell; a window containing both colors is dead (0), else table[own_count] - table[opp_count] with all table values explicit in configs/eval_v0_weights.toml; one stone updates <=18 windows (3 axes x 6 offsets); the Eval trait contract (apply/undo per placed stone, side-relative integer value) is the SAME trait the Stage-2 codebook net implements — cheapest eval that produces sane depth-4-6 play; sophistication is Stage 2's job — flips if depth-6 play is pathological in obvious ways (adjust weights, not architecture).

D-12: Test strategy: an independently written brute-force reference generator in the test tree is THE movegen oracle (full radius-8 region by scan, ordered enumeration, unordered dedupe, rule-4 truncation; exact count equality at depths 1-3 turns over origin + hand-built midgame fixtures incl. a two-cloud union case); sha-pinned golden boards pin rules 2 and 4 (exact-six per axis, overline-7, five-is-not-win, first-stone-win truncation, second-stone win); tactical-v0 = ~20 sha-pinned mate/must-block positions with a pass threshold pre-registered in the fixture header before the first run — Hard Rule 7 — flips never.

---

D-13: One cargo workspace, members `crates/*`, edition 2024, `publish = false`, and `unsafe_code` denied workspace-wide — one toolchain surface for eight crates, and unsafe becomes a visible, per-crate opt-out rather than something that arrives unannounced in a hot loop — a measured hotspot that needs unsafe, which then documents the opt-out in its own line here.

D-14: No `[profile.release]` tuning in WP-01 — codegen settings are a perf-sensitive change and rule 5 wants a pre-registered hotspot, an expected gain bracket and an IQR-gated bench; there is no code to bench yet — the first benched work package, which sets them with numbers attached.

D-15: Config is TOML, parsed by serde with `deny_unknown_fields` on every struct, no serde field defaults and no `Default` implementation anywhere — rule 1; a mistyped key must be an error, and a value exists because an operator wrote it — an explicitly optional section, which would need its own line here first.

D-16: `schema_version` is a required top-level integer checked against `SCHEMA_VERSION` — configs outlive binaries, and version skew must fail at load rather than at the first divergent behaviour — a specified migration path, which would replace the equality check with a range.

D-17: Parsing and validation are separate calls and the parse-only entry point is named `parse_unvalidated` — cross-field rules cannot be expressed in serde, and the blunt name makes a caller that skips validation visible at the call site — a typestate wrapper that makes an unvalidated config unrepresentable instead.

D-18: `MIN_TT_BYTES` and `MAX_CANDIDATE_RADIUS` are rejection bounds, not values — code never completes a config, it only refuses an impossible one, so a config that omits the key is still an error — a bound that becomes a tunable itself, at which point it moves into the schema.

D-19: `search.tt_bytes` must be a power of two and at least 1 MiB — the table will index by masking, and a size that is not a power of two would have to be rounded; rounding a stated value silently is the failure mode this project forbids — a cluster-based table with non-power-of-two bucket counts.

D-20: `search.candidate_policy.radius` is validated against its own ceiling and is never compared with the rules' radius-8 legal region — they are different concepts, one a search knob and one a game rule, and conflating them is the mistake CLAUDE.md rule 2 names explicitly — nothing; a coupling would need its own line here and a very good reason.

D-21: `eval.weights_file` is validated for shape, not existence — config validation stays pure, offline and deterministic, and a missing or corrupt weights file is pistol-eval's loud error at load time — a deployment gate that must catch a missing artifact before a match starts.

D-22: `Budget` is a closed enum, an absent budget is `EngineError::BudgetMissing`, a zero budget is a named config error, and instrument mode refuses `MovetimeMs` with `EngineError::InstrumentBudgetUnsupported` — wall-clock is not reproducible and instrument mode is the source of every strength claim (rules 1, 4, 6) — a deterministic simulated clock.

D-23: `EngineError` implements `Display` and `Error` by hand rather than taking a `thiserror` dependency — the enum is small and closed, and its text is part of the operator contract, so it is written rather than generated — enough variant growth that hand-written text starts to drift.

D-24: Deserialization failures become `EngineError::Config { key, why }` with the key recovered by `serde_path_to_error` and spliced with the member named in the message — every rejection has to name a key an operator can go and edit, and serde alone reports the container — a schema layer with native path reporting.

D-25: Config validation ships as the `pistol-engine` example `validate_config`, not a pistol-cli subcommand — pistol-cli's surface mirrors the `Engine` trait one to one (rule 11), and config checking is a tools-side gate — a CLI that grows a general maintenance surface.

D-26: `tools/ci.sh`'s fresh-build gate builds a checkout of the git index, not a clone of `HEAD` — the gate exists to catch a build that depends on an untracked file, and the index is the tracked set in every state: it equals `HEAD` on a fresh checkout and equals the about-to-be-committed tree when work is staged, so a clone of `HEAD` would pass a working tree that is about to break CI — anything the build needs that lives outside the index, such as a submodule or a generated file, which would make a clone the honest thing to build.

D-27: `tools/artifact_check.sh` applies rule 7's 10 MB per-file ceiling to every tracked file, not only to fixtures — no tracked file has any business being that large, and one rule is easier to obey than two — a legitimately large tracked file, which would need its own line here.

D-28: pistol-core carries no code in WP-01, the radius-8 constant included — a rules constant ships with its pinning test in the rules work package, and an untested constant that other crates start reading is worse than no constant at all — the rules work package landing.

D-29: The no-code-side-default guarantee is enforced by a `compile_fail` doctest on `Config` plus a source scan over `config.rs`, `validate.rs` and `budget.rs` in `config_rejects_code_side_default_probe` — a rule that only lives in a review checklist rots quietly; this makes reintroducing a default a red test — a lint or macro that can express the same guarantee directly.

D-30: `tools/ci.sh` runs `cargo fmt --all --check` as a gate — added post-WP-01 by operator instruction; style drift is review noise, and reviewers are here to flag correctness and requirement gaps — flips never.

---

D-31: pistol-core raises its own closed `CoreError` (OccupiedCell, OutsideLegalRegion, FirstStoneNotAtOrigin, GameDecided, UnoccupiedCell, NothingToUndo) and pistol-engine wraps it per D-10 — core is std-only and cannot name `EngineError`, and a rules refusal has to be a value the rules layer can return; the engine maps it at the call site, where `state.turn()` supplies D-10's `IllegalMove.turn`, and `NothingToUndo` maps to `InternalInvariant` because no protocol line can provoke it — flips if the two crates ever merge, which they will not.

D-32: Board occupancy is a `BTreeMap<Coord, Color>` ordered by (q, r) — determinism by construction: no hasher, no per-process random state, and iteration a caller can observe is the same lexicographic order the protocol and the search tie-break already use (D-5, D-7) — flips when a bench names occupancy lookup as a measured hotspot, at which point the replacement (fixed-seed open addressing) goes behind the same `Board` API and is a benched change under rule 5, not a contract change.

D-33: Axes are named for the coordinate that stays CONSTANT along them, and the name says so: `Axis::ConstQ` direction (0,+1), `ConstR` (+1,0), `ConstS` (+1,-1) — naming an axis for the coordinate that varies has no consistent answer for the third axis, and the `Const` prefix keeps the invariant in the identifier that eval windows, threat lines and pattern indices will read a thousand times rather than in a doc comment beside it — flips never.

D-34: Coordinates are i16 and every coordinate arithmetic site is `checked_add`/`checked_mul`/`checked_neg` with an unconditional panic naming `COORD_OVERFLOW`, never a bare operator and never `debug_assert!` — i16 addresses some four thousand turns of legal-region growth, so leaving the range is a bug in pistol rather than operator input, and a `debug_assert` would leave the release build (which has no overflow checks, D-14) silently wrapping — flips if an analysis mode ever accepts operator-supplied unbounded coordinates, which would validate at that seam instead.

D-35: `Board` is storage plus the rule-5 region query and does NOT enforce reachability; `GameState::place` is the single entry point that enforces the whole placement rule — the golden fixtures and the union-of-balls test need synthetic positions no legal game reaches, and a container that refused them would be lying about what it is; the mitigation is that `GameState`'s fields are private, the board is handed out by shared reference only, and there is no `board_mut` (pinned by a compile_fail doctest) — flips never.

D-36: Win detection is `wins_at(&Board, Coord)` reading the colour from the board, with no colour argument, no whole-board scan, and a `WIN_CHECK_ON_EMPTY_CELL` panic on an empty cell — a colour argument admits a mismatch class with no legitimate caller, rule 4 only ever asks about the completing stone, and the panic is unreachable from the protocol path because every refusal a line could provoke is named and returned before the win check runs (so D-5's stay-alive still holds); `winning_run` reports the first axis in `Axis::ALL` order and a run start defined as the end reached against the axis direction, so neither is "whichever the scan reached first" — flips if position validation needs a scan, which arrives as an additive validation-only entry point.

D-37: The golden-board fixture is a line-oriented text format read by a strict test-tree loader (unknown directive, missing verdict, or a `last` stone that is not on the board = a panic naming the line) and pinned by a SHA-256 computed in the test tree from a std-only implementation that is itself pinned against the published FIPS 180-4 vectors — pistol-core takes no dependency, dev-dependencies included, and the pin has to fire in the test that reads the fixture so that `cargo test` alone catches drift rather than only a shell gate; win cases additionally assert the position WITHOUT the last stone has no completed run, so the fixture pins rule 4's completing stone and not merely a board — flips if the workspace ever gains a vetted hashing dependency.

D-38: `GameState` keeps a per-ply record (cell, mover, prior phase, prior turn) and undo restores it rather than recomputing the transition backwards — the search applies and takes back one ply per node (D-9), and a backwards transition would be a second implementation of the rules sitting next to the first; the outcome needs no record because a stone is only ever placed from an ongoing game — flips never.

D-39: The `"q,r"` stone token is parsed by `FromStr for Coord` in pistol-core, beside the `Display` that writes it, and rejects surrounding space, a space after the comma, a leading `+`, an empty field and a second comma — the token is a protocol contract (D-5), and a formatter shipped without its parser guarantees pistol-cli and every fixture loader write their own, which then drift on exactly the cases nobody tests — flips never.

D-40: An empty board refuses any cell but the origin with `FirstStoneNotAtOrigin`, a different name from `OutsideLegalRegion` — rule 5 imposes nothing on an empty board (there is no stone to measure from), the origin restriction is rule 3's without-loss-of-generality normalization, and a diagnostic that cites the wrong rule is the quiet kind of wrong; a game recorded with a different first stone is replayed by translating every stone by the same vector with `Coord::offset`, which is geometry and already lives in pistol-core — flips never.

D-41: `Board::apply`/`undo` is the per-stone seam that WP-04's incremental zobrist and D-11's incremental eval hook, and `GameState` composes it rather than owning a parallel copy of the same state — incremental state has to sit at the layer that actually mutates the stones, or a caller holding a `&mut Board` desynchronizes it — flips never while `Board::apply` is public.

D-42: `GameState::from_plies` replays a move list through `place` and is the only way to reach a position other than by playing to it — D-6 requires that a position handed in from outside must not already contain a completed line, and replaying makes that fall out of the one implementation of the rules instead of adding a whole-board decidedness scan that D-36 declines to ship — flips if a fixture format ever needs to state a position that no legal game reaches, which would need its own line here.

D-43: Rule 4's truncation cases live in a second sha-pinned fixture of MOVE LISTS (`golden_games_v1.txt`), not in the board fixture, and the turn-model and undo tests read their scripted games from it rather than from arrays in the test files — D-12 promises the pinned goldens cover the first-stone-win truncation and the second-stone win, and both are statements about WHEN a stone was played, which a board cannot express; a script sitting in a Rust array is exactly the thing that can drift without anyone noticing, which is what pinning is for — flips if a case ever needs a position no legal game reaches, which belongs in the board fixture instead.

D-44: Amends D-34's letter, not its intent: coordinate arithmetic that stays in i16 is `checked_*` with a named panic, and widening to i32 is the sanctioned alternative where the result is not a coordinate and provably cannot overflow (`Coord::s`, `Coord::distance` — the differences of two i16 always fit) — D-34 says "never a bare operator", which reads as absolute and is contradicted by those two functions and by the `i16::try_from` on a run length in `winning_run` (which panics with `COORD_OVERFLOW` rather than expecting) — flips never; the rule is that no i16-resident coordinate can wrap, and widening is not wrapping.

D-45: `GameState::undo` takes the stone off the board BEFORE popping the ply history, and panics with `HISTORY_DESYNC` if the colour the board hands back is not the one the history recorded — a mutation followed by a failure would leave the state half-taken-back, and the board and the history are two representations of the same game that are free to check each other; the seam D-41 designates for incremental zobrist and eval has no other way to notice a desync — flips never.

D-46: The `"q,r"` stone token is canonical — no leading zeros and no `-0`, so `display(parse(t)) == t` for every token the parser accepts and each cell has exactly one spelling — the token is a protocol contract (D-5, D-39), and a grammar that accepts `007,0` and echoes `7,0` lets two lines that differ mean the same move, which is a difference an arena log, a fixture diff, or a dedupe by string will eventually trip over — flips never.

D-47: `Coord::checked_step` judges a step by whether the DESTINATION is addressable, computed in i32, not by whether the intermediate `direction * steps` fits in i16 — the intermediate test refuses cells that exist (stepping `i16::MIN` along `ConstS`, whose `r` component is -1, needs an `r` delta of 32768 while landing somewhere ordinary), and `None` is read by the line scan as "no cell, so no stone", so a false `None` is a wrong answer rather than a conservative one — flips never.

D-48: `winning_run` carries the run's far end out of the scan that already stood on it, instead of recomputing it as a step count from the run length — supersedes the clause of D-44 describing an `i16::try_from` panic in `winning_run`: a line of this lattice holds 65536 cells, so a run can be longer than a step count can express, and `Board::apply` can build such a position by design (D-35); the old code turned a diagnostic call into a panic at 32769 stones while `wins_at` answered correctly — flips never.

---

D-49: A turn is the value `Turn::Single(Coord) | Turn::Pair(Coord, Coord)` with the pair canonical (smaller cell first, lexicographic by q then r), and `Single` carries all three one-stone cases — turn 1 (rule 3), a stone that completes a line as a turn's first stone (rule 4), and nothing else — while a stone that completes a line as a turn's SECOND stone stays an ordinary `Pair` — one turn needs exactly one representation or perft cannot dedupe by value and the TT cannot key on it, and rule 4's truncated turn genuinely is a turn of one stone rather than a pair with a flag; the play order is not part of the value because it is a legality question (D-6) that `make_turn` answers — flips never.

D-50: The turn-level API (`generate_turns`, `make_turn`, `unmake_turn`, `perft`) is defined only at a turn boundary and refuses a half-played position with `CoreError::TurnInProgress`; the ply-level `GameState::place` is what finishes such a turn — `unmake_turn` has to be the exact inverse of `make_turn`, and in a history that can contain a lone second stone the grouping of plies into turns is ambiguous, so the ambiguity is refused rather than guessed — flips when a mid-turn `set_position` has to move, which adds an explicit finish-the-turn entry point instead of relaxing this.

D-51: `make_turn` plays the canonical ordering of a pair and the reverse only if the canonical one is refused — including refused by rule 4, where the smaller cell completes a line and that ordering would play a turn of one stone instead of this pair — the same turn must always leave the same ply history behind or the zobrist path (D-8), the incremental eval (D-11) and any future repetition accounting depend on how a caller spelled it; every refusal takes its own stone back first, so a refused turn changes nothing — flips never.

D-52: Generation enumerates the union of the radius-8 balls around the stones and emits a pair whose two cells are both in that base region from the smaller cell only — unless the other cell wins, whose own enumeration emits nothing for it (rule 4) — with the cells outside the base region reached only through the ball the first stone opens; it does not enumerate ordered placements and dedupe — the emission is then exactly output-sized with no set on the hot path, and the three facts that make it correct (a pair with both cells outside the base region has no legal ordering, a winning cell is never half of a pair, a cell outside the base region is empty) are the ones the brute-force reference pins — flips when Stage 1's threat-first staged generator supersedes it, which is an additive candidate policy over this rule-complete set, never a replacement for it.

D-53: `CoreError` gains `IllegalTurn { turn, why: &'static str }` with the reason as a named public constant, plus `TurnInProgress { turn }` — the whole-turn refusals (a pair of one cell, an uncanonical pair, a stone count the turn does not owe, both cells winning) have no single cell to name, and D-10's `EngineError::IllegalMove { turn, why }` passes the reason through unchanged; a refusal that IS about one cell keeps its own variant and names it, so `make_turn` reports `OccupiedCell`/`OutsideLegalRegion` rather than wrapping them — flips never.

D-54: Perft counts TURNS with `perft(0) = 1` and a decided position generating nothing, so a won line is counted where it ends and never expanded; full depth 2 is checked only where it is reachable (from the initial position, and at a decided position) and the second level elsewhere by an exact divide over a deterministic sample of first turns against the same reference — measured branching factors: 42768 turns with one stone on the board, 54228 with five, and around 1.7 * 10^5 one turn deeper, so two full levels from a midgame position is 10^9 to 10^10 turns and no test run enumerates it; `tools/perft_check.sh` is the perft gate and runs the wide sample in release — flips if a materially cheaper reference ever makes a full second level affordable, which would be a widening of the sample, not a change of oracle.

D-55: The perft fixture states positions as MOVE LISTS and pins its counts by SHA-256, and every pinned count is cross-validated against the brute-force reference in the same test run — the same argument as D-43 (a position written as a heap of stones cannot say whose turn it is or which stones belong to the current one), and a count pinned only against the implementation that produced it pins nothing; the two-cloud union case is a bridged dumbbell rather than disjoint balls because every stone is placed within eight of an existing one, so the region a legal game builds is always connected — flips if a fixture ever needs a position no legal game reaches, which would need its own line.

D-56: The turn token `"q,r/q,r"` ships with its parser now rather than with the line protocol, and the parser refuses an uncanonical or doubled pair instead of repairing it — D-39's argument (a formatter without a parser grows its second implementation in whichever crate needs one first) and D-46's (one turn, one spelling, or two protocol lines that differ mean the same turn) apply unchanged to the pair token that D-5 already pinned — flips never.

---

D-57: The zobrist seed is `ZOBRIST_SEED = 0x706973746F6C0001` (`"pistol"` and a key-format version), and a key is two SplitMix64 streams over one 64-bit word `tag << 56 | payload`, the high stream seeded by the mix of the low one so that both descend from the single seed and no second magic constant is chosen; the tag separates the cell, side and phase families, and a cell's payload is its two `i16` coordinates in two's complement plus the colour — the word is injective and SplitMix64 is a bijection, so distinct inputs cannot share a key in the full 128 bits *or* in either half alone, which is the property the TT's 64-bit verification word (D-8) rests on and which a table of drawn random numbers would only have probabilistically — flips on a demonstrated weakness of SplitMix64 as a keyed hash, which would change the mixer and the version digit of the seed together and rename every position in every book, net and log keyed by the old one.

D-58: `Board` carries the stones' key incrementally (one XOR in `apply`, one in `undo` — the seam D-41 designates) and `GameState::key` composes it with `side_key(to_move) ^ phase_key(phase)` on read, rather than storing a full key that the transitions toggle — the side and the phase are already fields of the state machine that undo restores exactly (D-38), and a stored full key would be a second copy of them that can fall out of step in silence, while two XORs on read cannot; the turn number and the outcome are deliberately absent from the key, because in an ongoing game the stone count fixes the turn, the phase and the mover together, and a completed run is a property of the stones — flips if a bench names the composition as a measured hotspot, which is a rule-5 change behind an unchanged `key()`.

D-59: No key cache, though D-8 permits one — two SplitMix64 mixes are a few cycles and cheaper than any map probe that could memoize them, and rule 5 wants a pre-registered hotspot and an IQR-gated bench before a perf structure exists at all; `zobrist_key_is_pure_function_of_cell` asserts exactly what a cache would have had to preserve, so adding one stays a benched change rather than a redesign — flips on a bench that names `cell_key` as a hotspot.

D-60: The pinned key vectors in `tests/zobrist_key_tests.rs` were computed outside the implementation from D-57's construction, with the SplitMix64 they used checked against Vigna's published stream for state zero — a vector copied out of the code it checks pins nothing (D-55), and cross-process determinism (rule 4) is precisely the claim that a value computed elsewhere matches; the other half is the seeded-playout round trip, which checks the carried key against a from-scratch recompute over more than a thousand applied and taken-back stones and names a reproducible playout when it fails — flips never.

---

D-61: The `Eval` trait is exactly `apply(cell, colour)` / `undo(cell, colour)` / `value(side_to_move) -> i32`, object safe, total, and with the colour passed in rather than read off a board — the caller that moves the stone is the caller that updates the eval (D-41), a colour argument means an implementation never needs a board reference and can cross-check what it is told, an infallible signature is honest because every refusal a caller could provoke is already `GameState`'s (rule 3), and object safety lets the engine hold a config-selected backend without making every search type generic; the value depends only on the SET of stones applied, so neither apply order nor take-back order is part of the contract — flips if a backend needs whole-board context it cannot carry incrementally, which adds a sync-from-board entry point rather than changing these three.

D-62: v0 window bookkeeping is a `BTreeMap<Window, Counts>` keyed by `(axis, start)`, with an entry present exactly while the window holds a stone and pruned the moment it empties, and a carried `i64` sum from black's point of view — an unbounded board has infinitely many empty windows and none of them score, so the map is the position's whole eval state; pruning is what makes an unwound eval byte-identical to a fresh one, which `eval_apply_undo_roundtrip` asserts by value equality rather than by re-reading a score, and the `i64` means a position summing far past the band clamps rather than overflowing — flips when a bench names window lookup as a measured hotspot, which is a rule-5 change behind an unchanged trait.

D-63: A window holding `WIN_LEN` own stones scores `DECIDED_WINDOW_VALUE = EVAL_MAX` (16000) and is deliberately NOT a document entry — the config table stops at 5 because six own stones is a win, and a win's score is the search's mate band with a distance in turns (D-3), which no number in a weights file can express; eval stays total because the winning stone is on the board before the search asks anything, so it saturates the band instead of panicking — flips never; the value of a win is D-3's business and not this table's.

D-64: The eval weight table is its own document kind — `schema_version` + `backend` + `[table]` with keys `1`..`5` — with its own version and its own validator (the pistol-eval example `validate_weights`), and `tools/config_check.sh` tells the two kinds apart by the `_weights.toml` filename suffix — a weight table checked against the engine config schema is rejected for every key it does not have, which is the note WP-05 inherited from D-21's deferral; the `backend` key is what stops a Stage-2 codebook file from being read as a v0 table, and a misnamed weights file fails loudly as a config rather than passing silently — flips if a third document kind makes the suffix rule cramped, which would put an explicit kind key in every document and bump both schema versions together.

D-65: Weight values are rejected unless `table[1] >= 1`, the table strictly increases, and `table[5] <= EVAL_MAX` — bounds, not values (D-18): more own stones in a window is never worth less, and one window that alone saturates the clamp would make every deeper distinction invisible; the committed values (2/12/60/300/1500, marked OPERATOR-CONFIRM) are Stage-0 sanity numbers that Stage 4's SPSA/Texel replaces, and an edit to them is a strength claim under rule 6 — flips if a tuner demonstrates under SPRT that a non-monotone table plays better, which relaxes the bound with the evidence attached.

D-66: `validate_config --check-weights-file` performs the weights-file existence check D-21 defers, in the tools-side example rather than in `Config::validate` — D-21's flip clause is exactly this deployment gate; keeping it out of `validate()` keeps validation pure, offline and independent of the working directory, and the gate checks only that the file is there and readable, never its contents, which have a different schema and a different validator (D-64) — flips never; an engine that must refuse a missing artifact at load checks it at backend construction, not in config validation.

D-67: The eval window (length `WIN_LEN`, three axes, 18 per cell) is enumerated in pistol-eval, not pistol-core — a window length is a property of an eval backend and not a rule (Stage 2's codebook reads length-11 windows under the same trait), while the geometry it composes (`Axis::direction` through `Coord::checked_step`) still comes from core, so no rule is restated (rule 2); a window needing a cell outside the addressable lattice is not a window and is skipped, which is D-47's reading of `None` as "no such cell" — flips if Stage 1's threat generator needs the same enumeration, which moves this module into core rather than copying it.

D-68: The from-scratch eval reference lives in the pistol-eval test tree and is itself checked two ways — the cheap stone-driven enumeration against the incremental value at every ply of seeded playouts (over a thousand stones, down and back up), and that reference against a full bounding-box region scan on fixed positions — the cheap reference shares D-12's oracle role but also shares the implementation's central assumption (only a window holding a stone can score), and the region scan is the independent statement of it, too slow to run per ply and unnecessary there — flips never.

D-69: pistol-eval carries its own small copy of the `serde_path_to_error` key-splice that D-24 describes for the engine config — the dependency runs engine -> eval, so eval cannot borrow the engine's mapper, and a shared utility crate is not in the crate map (D-1); thirty duplicated lines is cheaper than either inverting a dependency or adding a ninth crate, and both copies are pinned by their own rejection tests — flips if a third crate needs the same mapper, at which point it earns a home rather than a third copy.

D-70: The eval's desync checks are opportunistic and say so: `apply` catches a window that would exceed `WIN_LEN` stones and `undo` catches a window that holds no stone of the colour being taken back, both panicking with `EVAL_DESYNC`, and neither mirrors the board's occupancy to catch every drift — a `Coord -> Color` map inside the eval would be a second copy of the board on the hot path, which is a cost rule 5 wants benched rather than assumed, and the real guarantee is the recompute-at-every-ply playout test (D-68) plus the fact that `GameState` refuses an occupied cell before an eval ever hears about it — flips if a search bug class turns up that pairs apply and undo wrongly in a way the playout test cannot see, which buys the occupancy mirror with a bench attached.

---

D-71: The search's internal unit is the ply and every unit it reports is the turn, and a root in the middle of a turn is refused with `SearchError::TurnInProgress` rather than searched — the depth, the budget, the mate distance and the principal variation are all turns, and a line that begins half way through one cannot be grouped into `Turn` values without inventing a one-stone turn the rules do not have (D-49); the ply-level `GameState::place` is what finishes such a turn, which is the same answer D-50 gives at the turn-level API — flips when a mid-turn `set_position` has to be searchable, which would report the opening half-turn as its own kind rather than relax this.

D-72: A mate distance counts EVERY turn from the root, both sides', so `MateIn(1)` is exactly D-3's "the side to move completes six this turn", a win for the side to move is always an ODD distance and a loss always an even one, and both plies of a turn carry the same distance because they carry the same turn number; the table re-bases a stored score by `state.turn() - root_turn` on the way in and out — the alternative, counting only the winner's own turns, makes `MatedIn` count the other side's turns and leaves the re-basing arithmetic half a turn wide, while rule 4 already scores sudden death in game turns — flips never.

D-73: The search's stop condition is its own closed enum `Stop { DepthTurns | Nodes | Deadline }` and the engine translates `Budget` into it at the composition root — which budgets a mode may use is operator-facing policy and stays in the engine (D-22), while stopping is the search's; the translation is not a copy, because `MovetimeMs` becomes the `Instant` it expires at, so the duration-to-deadline clock read happens in the engine and the search holds no path that computes a deadline of its own (CLAUDE.md rule 4) — flips if a search ever has to re-derive a deadline, which would take the clock as an injected parameter rather than reading it.

D-74: The stop condition is tested every `NODE_CHECK_INTERVAL` (1024) nodes at a node boundary, so a search given n nodes stops on node `n.next_multiple_of(1024)` on every machine and in every run, and the FIRST iteration is not interruptible — a search that returned no move would be the silent failure rule 3 forbids, so a budget below the cost of one full-width first iteration overshoots, and the overshoot is reported in `nodes` rather than hidden; the granularity is a pinned constant and not a tunable, because two runs at the same node budget have to be comparable — flips if a partially searched first iteration ever becomes usable, which needs a root that can answer with the best move found so far.

D-75: The transposition table is a power-of-two count of four-entry buckets of 24-byte entries, sized to the largest count that FITS inside `search.tt_bytes`, with `Table::bytes()` reporting what was taken — a 96-byte bucket never divides a power-of-two budget, so up to a quarter of it goes unused; that is a ceiling honoured rather than a stated value silently rounded (D-19), and for a power-of-two budget it yields exactly the entry count a padded 32-byte entry would have — flips if a non-masking index (a fixed-point multiply) benches at least as fast, which would spend the whole budget on entries.

D-76: Move ordering scores a candidate by applying the stone to the `Eval` backend, reading the value and taking it back — the evaluation IS the static pattern score, and a second opinion living in the search would be a second pattern implementation to keep in step; the cost is one apply/undo pair per candidate per node, which is this work package's registered hotspot and the first thing a Stage-1 bench should attack (an incremental per-cell score under the unchanged trait of D-61) — flips on that bench.

D-77: A candidate set is the policy's cells intersected with pistol-core's own legality answer, asked one cell at a time; the two radii are never compared, so a policy radius wider than the rules' region simply yields the region and this module never learns why, and an empty board is not a special case but the honest reading of a proximity policy — there is no stone to be near, so the policy restricts nothing and rule 3 decides alone — the shortcut this refuses ("the radius is at most eight, so every policy cell is legal") is precisely the comparison D-20 forbids — flips never.

D-78: No transposition cutoff is taken at a principal variation node (one whose window is wider than a single point), so a reported line always runs to the horizon or to a win — a cutoff returns a score without the line that proves it, and the line is what the protocol prints and what the cross-process determinism gate diffs (D-7) — flips if a bench shows the cutoffs are worth a shortened line, which would then have to be recovered from the table and would no longer be the line the score was proved on.

D-79: The search enumerates plies, so it reaches each pair twice, once per order, and relies on the transposition table to collapse the two at the node below; it does not restrict the second stone to cells above the first — a pair whose smaller cell is only reachable *after* the larger one is placed is legal (D-52), and a canonical-order restriction at ply level would silently drop it — flips with Stage 1's threat-first staged pair generation, which generates turns rather than plies and pays the dominance test once.

D-80: An iteration the budget interrupts is discarded as an answer and kept as work: the returned `SearchInfo` carries the last completed depth's line, score and depth, and the whole search's nodes, time and table occupancy — half a depth is not a depth, but the nodes were really spent, and per-side compute is a reporting requirement (CLAUDE.md rule 6) — flips never.

D-81: WP-06's measured Stage-0 floor, recorded as a finding rather than treated as a failure (CLAUDE.md rule 5): from the opening position on one core of the development machine, a release build reaches depth 6 turns in 9.5 s at candidate radius 1, depth 5 in 73 s at radius 2, and depth 4 in 27 s at radius 3 (the committed value), at 150k-230k nodes per second; inside the 0.5 s deployment budget that is depth 3 turns at radius 3 and depth 5 at radius 1 — full-width ply enumeration with no quiescence, no threat generation and no reductions is what the research report predicts, and the depth this buys is exactly the argument for Stage 1's threat-first generation, killers and history rather than for a wider radius — flips with the first Stage-1 change that moves it, which arrives with its own bench and SPRT.

---

D-82: The `Engine` trait is D-2's three verbs plus `go_reporting`, `mode()` and `state()`, with `go(Budget) -> SearchOutcome` kept exactly as D-2 pinned it and implemented as a provided method over `go_reporting(Budget, &mut dyn FnMut(&SearchInfo))` — the protocol prints one `info` line per completed depth and a return value cannot carry a stream, while the alternative (a `SearchOutcome` carrying every report) needs no trait change but leaves a long search silent until it ends; `mode()` is what the handshake reports and what decides which budgets are honoured, and `state()` is the read side of `set_position` that lets a driver check its own game against the rules' own state rather than a second opinion (rule 2) — flips when the API spec requires cancellation, which is D-2's own flip clause.

D-83: `EngineError` gains `PositionNotSearchable { why }`, amending D-10's variant list: a `phase:1` position is legal, reachable and one the protocol must accept (D-6), and the search refuses a half-played root (D-71), so the engine needs a name for "this position exists and there is nothing here to search" — calling it `IllegalPosition` would say a position that exists is impossible. D-71's own flip clause (make a mid-turn root searchable) is deliberately not taken, because every unit this engine reports is a turn — flips if a mid-turn `set_position` ever has to be searchable, which is D-71's problem and not this variant's.

D-84: The engine holds only searchable positions: `PositionSpec::replay` refuses a decided position in BOTH forms with `IllegalPosition`, as an explicit check after the replay. This amends the letter of D-42: `GameState::place` tests the outcome on entry, so the ply that completes a line returns `Ok` and `from_plies` returns a decided state — "a move after the game was decided is refused by name" is true only of a move *after* the winning stone, and a move list ending in a win is accepted by the rules layer. The work package pins the refusal for the `set` form; the move list gets it for the same reason (rule 4 makes a won position terminal, so there is no move to ask for). The cost is that a finished game record cannot be loaded, which no caller needs: a driver plays turn by turn and stops when its own state is decided — flips if an analysis mode has to stand on a finished game, which would need a read-only entry point rather than a relaxation here.

D-85: `position set` lists each side's stones in PLAY ORDER, and the replay asks the state machine whose stone comes next (`to_move`, `stones_owed`) instead of computing the interleave itself — the turn structure is rule 3's and lives in pistol-core (rule 2), so the engine holds no copy of it; a complete turn goes through `make_turn`, which means a pair that is legal in only one order (D-52) is accepted whichever way round it is written (D-51) and leaves the same ply history the move list would; a side with fewer stones left than its turn owes is the phase-1 case, and that one stone is played by the ply-level `place`; the stated `tomove` and `phase` are checked against the replayed state rather than trusted, so a document that contradicts itself is refused and never repaired — flips never; a form with unordered stones would have to search for a legal order, which is a different problem.

D-86: Refusals split by form: a rejected turn in a move list is `IllegalMove { turn, why }` with the turn number read off the state (D-31's shape), and anything wrong with a stone list is `IllegalPosition { why }` naming the PLY it happened on — whoever wrote a stone list named no turn, and answering with a turn index they never wrote is a diagnostic that cites the wrong thing; `SearchError::GameDecided` maps to `InternalInvariant` rather than to a refusal, because D-84 means the search can no longer be handed a decided root and an unreachable path should say so — flips never.

D-87: pistol-cli gains a library target and ONE binary, `pistol`, with `perft` and `selftest` as subcommands — superseding the crate map's four-binary reading and narrowing D-25's argument to "the protocol binary's I/O mirrors the `Engine` trait one to one": the protocol is the contract the future API layer adapts (rule 11), so it has to be exercised in-process rather than only through a pipe, and one binary means the determinism gate compares two runs of the same engine-construction path instead of two programs that build engines differently. `bench` is still absent and is still named, in that binary's own help, because a benchmark ships with the first perf-sensitive change and its pre-registered hotspot (rule 5, D-14) — flips if a command ever needs a dependency the protocol does not.

D-88: Protocol answers, pinned: quiet success for `newgame` and `position`; exactly one `error <NamedError>: <why>` for every refusal, with the explanation from `EngineError::detail` so the kind is not stated twice; one `info` line per completed depth whose field set is exactly `depth_turns seldepth nodes nps time hashfull score pv` in that order — `hashfull` included, which the work package's sketch does not name and which is safe for a driver that keys on names rather than positions — then one `info totals …` line whose marker distinguishes it from the depth it repeats with the whole search's cost (D-80, and per-side compute is a reporting requirement under rule 6), then `bestmove <turn>`; `id` lines are name, version, protocol, mode and budgets, plus whatever the binary adds (the config path, the eval backend, `tt_bytes`, the candidate radius) so that a transcript records the instrument a claim was measured with; scores are `cp <n>` in pistol-eval's own integer units and `mate T` / `-mate T` in D-72's turn distances; the budget kinds the handshake advertises are derived by asking `Budget::check_supported`, never restated. On the input side the `set` form's grammar is as strict as the tokens it carries (D-46's argument at the scale of a line): the four sections come in one order, each exactly once, all required, with a section's first value attached to its prefix, so `b: 0,0` and `tomove:` before `w:` are refusals rather than spellings. Input is read as BYTES and a line that is not UTF-8 is a `Protocol` refusal — a lossy decode would repair input silently and an early return would let one byte end a match — and a refused line is quoted back truncated (or hex-dumped, when it was not text), because a refusal is one readable line and not an echo service — flips if a driver needs a machine-readable framing, which is an additive line kind rather than a change to these.

D-89: `configs/gate_v0.toml` is a second committed instrument config at candidate radius 1, and it exists because of a measurement rather than a preference: at the deployment radius 3, a completed depth of 3 turns costs 84-100 s per fixture position and depth 4 costs hours, while a node budget costs the same at any radius (the measured table lives in that file). So the determinism gate and the fixture's deep cases run there, every shallow tactical case runs at `instrument_v0.toml`, and each fixture case names its own config so that no radius-1 result can be mistaken for a deployment claim (rule 6). This amends D-7 by fixing what that gate runs: this config, the work package's budgets `{depth_turns 4, nodes 200000}` unchanged, and `newgame` before every position so that no answer depends on the ones before it — flips with the first Stage-1 change that moves D-81's floor, at which point the gate moves back toward the shipping radius.

D-90: The determinism gate is three runs, not two: A and B are the same input to two processes (the law itself), and C is one process per position under the node budget, compared against the same positions in one session — the only cheap test that `newgame` clears everything the previous position left in the table, which two identically ordered runs cannot see. Each transcript is also checked for positive content — one `bestmove` per `go`, no `error` line, a completed depth on every answer — because two processes that refused every line would diff clean and certify nothing (rule 3); a same-process double run through the seam is `selftest`'s job, and a repeated `go` on an unchanged position is deliberately NOT required to reproduce its own node count, because the table it just filled is the point of having one — flips never.

D-91: The tactical fixture states a config, a budget and its expectations PER CASE, with `require <n>` once, pre-registered at every case before the first run of the suite. One hundred per cent is the honest threshold rather than an optimistic one: a full-width Stage-0 search is exhaustive inside its horizon, so within a budget that completes the depth a case needs it finds a forced win with certainty, and the only two ways to fail — a budget too small, or a candidate policy that excludes a cell the answer needs — are fixed in the file rather than left to the day. The mate-in-three cases were checked at radius 3, where white's defences are inside the policy, so what they pin is a game fact and not an artefact of a narrow search, and two of the three are the first one's images under a reflection and a rotation of the lattice. `expect move` is stated only where the winning TURN is unique — which is rarer than it looks, because where a single cell completes six the turn is not unique at all: a pair whose *second* stone takes that cell wins on the same turn, so those cases state `expect cell` (the cell every winning turn must contain) plus the score, and only the two-stone cases, where exactly one pair completes six and no single stone does, state `expect move`. An earlier revision of the file did state `expect move` on the single-stone cases; they passed, and would still pass, but only because a completed window saturates the evaluation band (D-63) and is therefore ordered first — which is this eval's tie-break rather than a rule of the game, so a Stage-4 retune could have turned a tuning change into what looked like a tactical regression. The threshold did not move. The suite runs in release from `tools/tactical_check.sh`, with the cheap cases and the fixture's pin also in `cargo test`, which is D-54's split; the pin is computed by a second copy of pistol-core's test-tree SHA-256, which carries the FIPS vectors with it so that a copy that drifted fails on its own (D-37, D-69's argument) — flips when Stage 1's VCF/VCDT fixture class arrives, which will pre-register below 100% and say so.

D-92: `Pistol::from_config` refuses `instrument.threads != 1` in every mode and matches exhaustively on `instrument.tie_break` — a config key no code reads is an instruction that silently did nothing (rule 3); this build searches single-threaded whether the mode is play or instrument, and the exhaustive match makes a second `TieBreak` variant a compile error at the composition root rather than a document that loads and is ignored — flips when Lazy SMP lands in Stage 4 and `threads` becomes a number the search consumes.

D-93: `configs/play_v0.toml` is committed beside the instrument config, identical but for `mode = "play"` — CLAUDE.md's design point is a strong move within 0.5 s, which is a `movetime` budget, and instrument mode refuses one by name (D-22), so the mode that honours it needs a document an operator can point at and `tools/config_check.sh` can validate; play and instrument state the same search deliberately, because a measurement of a differently configured search says nothing about play (rule 6) — flips never; the two documents diverging is a strength claim and needs its own line here.

D-94: A won line records the turn it wins on in `seldepth_turns` — the win branch of the recursion advances it before returning the mate score, because no child node is visited to do it there. Without that, a mate found on the first root candidate reports a completed depth of one turn beside a selective depth of none, which contradicts what `SearchInfo::seldepth_turns` promises and appears verbatim in a protocol transcript; the alternative (report `max(seldepth, depth)`) would be worse, because a mate found at turn 1 of a depth-5 iteration genuinely did not search five turns deep — flips never; a search that extends past its horizon will raise seldepth by the same rule, at the node that goes deeper.

D-95: Measured Stage-0 finding, recorded rather than fixed (CLAUDE.md rule 5): a `movetime` budget is a floor plus one full-width first iteration, not a ceiling. D-74 pre-registers the mechanism — the first iteration is not interruptible, because a search that returned no move would be the silent failure rule 3 forbids — but not the magnitude, and the magnitude is operator-visible: on the development machine, in release, at `configs/play_v0.toml`'s radius 3, `go movetime 500` on a game whose stones sit eight apart along one axis returns after 1.9 s at 10 turns, 8.6 s at 20 and 58.6 s at 50, because a first iteration costs on the order of the square of the candidate count and that count grows with how far apart the stones are — which is partly the OPPONENT's choice. So D-93's "the mode that honours it" means "the mode that accepts it", CLAUDE.md's 0.5 s design point holds near the opening and not deep into a spread-out game, and a driver on a hard clock must budget by nodes or depth in Stage 0. What flips it: the first Stage-1 change that makes iteration one cheap enough to be a rounding error (threat-first candidate generation) or a root that can answer with the best move found so far, at which point `movetime` becomes a bound and this line is superseded with the new numbers attached.

D-96: Amends the wording of D-52, not its design: its summary phrase "a winning cell is never half of a pair" reads as the opposite of what generation does and what `movegen`'s own module documentation says. A winning cell IS half of pairs — the ones where it goes down second, which rule 4 leaves an ordinary two-stone turn — and 367 of them exist in the fixture's `mate_in_1_five_in_a_row_blocked_at_one_end` position, all counted (a red-team differential against an independently written ordered-placement generator agrees with `perft --depth 1` exactly: 93 967 turns). What is true, and what the argument needs, is that the enumeration ROOTED at a winning cell emits nothing, because from there the turn truncates and the pair is not this turn — flips never; the code and its module doc were already right.

D-97: `perft(state, 0)` refuses a half-played position instead of answering 1 — amends D-54's "perft(0) = 1", which is true at a turn boundary and was the only door into the turn-level API that D-50 had not locked: every other depth raises `TurnInProgress` there, so a count at depth 0 was a number for a position perft is not defined on (CLAUDE.md rule 3's skip-with-default). Nothing the fixture states changes, because every fixture position is at a turn boundary, and `perft --depth 0` from the CLI now refuses the same positions `--depth 1` does — flips never.

---

D-98: Operator-accepted, and amends D-3's "distance in TURNS" by saying whose turns are counted: a mate distance counts EVERY turn from the root, both sides', exactly as D-72 implemented it, so D-3's `MateIn(1)` stays exact, an ODD distance is a win for the side to move at the root and an even one a loss, and `search_finds_forced_mate_in_2_turns` asserting `MateIn(3)` is correct as written. The protocol prints that same root-anchored number — `score mate T` and `-mate T` are not counts of the winner's own turns — and the convention is stated in the binary's `--help` beside the budget notes as well as beside `score_token`, because a driver that assumed the other convention would halve every distance it reads and nothing in a transcript would say so. The rejected alternative is D-72's own: counting only the winner's turns makes `MatedIn` count the opponent's and leaves the table's re-basing half a turn wide — flips never; a consumer that wants the winner's count derives it, and this engine will not print two conventions.

D-99: REVIEW-design, REVIEW-impl and RED-TEAM are dispatched as SUBAGENTS with fresh contexts, the implementing session never reviews its own work, a work package is not landable while its reviews are outstanding, and a session that cannot dispatch subagents says so and stops after IMPL for the operator to launch them — operator instruction closing the gap WP-05 and WP-06 both reported: CLAUDE.md's Process line named the review steps and required a fresh context, but not WHO runs them, and the one reading that costs a session nothing is to review itself, which is exactly the independence the step exists to buy; the amendment is in CLAUDE.md's Process section, and the outstanding reviews (WP-06 first — the PVS recursion, mate re-basing, TT interaction and abort semantics — then WP-05) run against the landed Stage-0 commits before Stage 1 builds on them — flips never; a harness that could prove independence another way would be a subagent under a different name.

D-100: The player enum is `Player { P1, P2 }` — P1 moves first and places the origin stone, P2 replies — and every identifier, doc, test name, fixture directive and protocol token that said black/white now says p1/p2: the `set` form is `position set p1:<q,r> … p2:<q,r> … tomove:<p1|p2> phase:<0|1>`, the golden and perft fixtures state `p1`/`p2` directives, and the eval's carried sum is P1-relative. Operator ruling, on evidence rather than taste: the HeXO platform speaks p1/p2, hexo-bridge maps platform p1/p2 onto the htttx engine alphabet x/o at its own boundary, and sealbot uses A/B — chess colour names name nothing on a lattice with no board edge and no first-move colour convention, and the rename lands BEFORE any adapter so nothing external ever binds to the b/w tokens (the future adapter maps p1<->x, x opens as p1 opens, and that mapping lives in the adapter alone — pistol never speaks x/o). Two things deliberately did not move: `player_index` still encodes P1 as 0 and P2 as 1, so D-57's key construction is untouched and every pinned zobrist vector still holds, and the four fixture SHAs were re-pinned in the same commit as the token change (D-37), which is the expected consequence of editing a golden file and not a drift. This file is append-only (its own preamble says so), so D-1..D-99 keep the word they were written with: read "black" as P1 and "white" as P2 wherever they appear above — flips never; a third alphabet would be an adapter's problem, not this crate's.

D-101: Operator-confirmed against the htttx spec and the HeXO server: rule 5's radius-8 legal region IS a rule of the game the platform enforces, not a pistol-local assumption, so `LEGAL_RADIUS` stays a pinned constant in pistol-core and nothing in CLAUDE.md's rules, the movegen, the perft oracle or the pinned counts moves. The question was worth asking and is recorded here rather than left open: rule 5 appears nowhere in hexo-bridge, sealbot's rules, or anything the platform side exposes, sealbot plays colony moves that can sit further than eight from every stone, and the HeXO server is stated to own legality — so the absence of the rule from the adapter's surface reads as evidence against it, and an engine that fails loud on a legal opponent move forfeits. It does not: legality is the server's to enforce and it enforces this. The demotion this answer declines would have been expensive and is worth naming so nobody re-derives it — `LEGAL_RADIUS` is read at some fifty sites across core's movegen, turn, board and error paths, the brute-force reference and the playout helpers, and every pinned perft count and fixture sha is radius-8-dependent — flips on a platform game in which an opponent's accepted move sits further than eight from every stone, which is a rules-truth amendment and not a bug report.

D-102: `MAX_TT_BYTES = 1 << 36` joins D-18's rejection bounds, and `Table::new` asks for its allocation with `try_reserve_exact` rather than taking it with `vec!` — a red-team finding, and the failure it replaces was not a refusal at all: `search.tt_bytes` was bounded below and by power-of-two but not above, `Table::new`'s only ceiling was a `usize::try_from` that cannot fail on a 64-bit target (so its named error was dead code), and a document identical to `configs/gate_v0.toml` but for `tt_bytes = 2^40` passed `validate_config` with `ok` and then ended the process in `handle_alloc_error` — a core dump where CLAUDE.md rule 3 requires one readable line naming an editable key. The split is deliberate: the pinned ceiling catches the typo class offline and deterministically, which is all config validation may do (D-21, D-66 keep it pure and machine-independent), while how much memory THIS machine has is a question only the constructor can ask, so the constructor asks it and names `search.tt_bytes` when the answer is no. The zeroing cost `try_reserve_exact` + `resize` pays over `vec!`'s `alloc_zeroed` was measured at the committed 256 MiB table and is inside the noise (22 ms either way), so no rule-5 bench is owed — flips if a deployment legitimately wants more than 64 GiB of table, which moves the ceiling and says what machine asked for it.

D-103: Amends D-65's letter, not its reason: the bound on a stated weight is `table[n] < DECIDED_WINDOW_VALUE`, strictly, where D-65 wrote `table[5] <= EVAL_MAX` and the two constants are the same number — so the bound as written admitted exactly the table its own stated reason forbids. Found independently by both WP-05 reviews. What the `<=` let through: `table.5 = 16000` validated, `validate_weights` reported ok, and `window_value(5)` then equalled `window_value(6)`, which is the one step in the table that separates one-from-a-win from a win; measured downstream, `Position::static_score_after` returned 16000 for a winning cell and 16000 for an irrelevant one, the ordering fell back on the coordinate tie-break, and the winning ply no longer sorted first — falsifying what `ordering.rs`'s own doc claims and what D-91's tie-break argument leans on. The structural cause is that the monotonicity loop walks the five STATED entries and the sixth is spliced in afterwards, so nothing ever compared `table.5` with the entry above it; the strict bound is that comparison. This is a bound and not a value (D-18): the committed table's 1500 is untouched, so no strength claim is made or needed (rule 6), and the case is now pinned at the boundary itself rather than one past it — flips if a tuner demonstrates under SPRT that the flat step plays better, which would be D-65's own flip clause with the evidence attached.

D-104: The interior empty-candidate branch is a horizon at a turn boundary and a named invariant (`NO_CANDIDATES_MID_TURN`) half way through a turn — it had neither an ADR line nor an honest behaviour, which is how two reviewers found it. What the source said was that a policy running dry is "a horizon rather than a rules statement" and that "the honest answer is what the position is worth". At phase 0 that is true. At phase 1 it is not: the node returns with its principal-variation slot cleared, the phase-0 parent promotes a line of one ply, and the line that reaches the root ends on a lone stone, which `turns_from_plies` refuses with `PV_NOT_PLAYABLE` — a panic at the root naming a symptom, arbitrarily far from the node that caused it, where CLAUDE.md rule 3 wants the refusal where the decision was made. There is no score that fixes it, because the missing thing is not a value but the mover's second stone. The branch is unreachable for any radius of at least one (a cluster's own boundary stones always have empty neighbours; a fully packed radius-2 ball still yields 18 candidates at radius 1), so this changes no search — but it is written for exactly the extension ROADMAP Stage 1 schedules first, the threat-first staged generator that SUPERSEDES the radius policy (D-52, D-79), and an extension axis CLAUDE.md's design point requires to stay cheap cannot have its documented behaviour be the opposite of its real one — flips when a candidate policy that can run dry mid-turn arrives, which must answer inside a turn and would replace this assertion with that answer.

D-105: Amends D-63's claim, and registers the behaviour change it implies as a Stage-1 experiment rather than making it now. D-63 says a decided window "saturates the band instead of panicking"; what the code guarantees is narrower and the difference is measurable. `DECIDED_WINDOW_VALUE` is one addend in a free sum, so a position holding a completed six for P1 plus enough unrelated P2 material evaluates at -EVAL_MAX for the side that has already won (measured: six P1 stones on one axis and seventy scattered P2 stones). Totality — D-63's actual subject — is intact, and no score crosses into the mate band (D-3), so nothing about the search is wrong today: D-84 keeps a decided position out of the engine, and the win branch returns a mate score before any node evaluates one. The live surface is move ordering, where `static_score_after` is this same unfloored sum and `value()` clamps, so in a position already at the band every candidate returns the same number and the ordering falls back on the coordinate tie-break. Flooring a decided window instead would change the eval's output, therefore move ordering, therefore playing strength — a rule-6 claim needing SPRT, which needs the arena Stage 1 lands first. So: this line is the amendment, the floor is pre-registered in docs/ROADMAP.md as a Stage-1 arena experiment, and D-63's sentence is superseded by "a decided window is worth the band, and the position around it still sums" — flips with that experiment's verdict.

D-106: The search gets an independent full-width negamax reference in its own test tree, scheduled as the first Stage-1 work package rather than written now — three reviewers raised its absence and two of them built one to answer their own briefs, both finding zero divergences against `Searcher` (44 fixtures over two depths, two radii and two table sizes in one case; nine position-depth pairs covering both `Eval` and `MateIn` results in the other). CLAUDE.md rule 7 makes an independently written brute force THE oracle for movegen and D-68 does the same for the eval; the search is the one component here whose correctness argument is subtle — the same-side window that D-9 calls the main pruning lever, D-78's no-cutoff-at-a-PV-node rule, D-79's reliance on the table to collapse the two orderings of a pair — and it is checked only against hand-reasoned mates and against two runs of itself. It waits for Stage 1 and not longer because everything Stage 1 adds is a PRUNING change and rule 6's SPRT judges strength, not soundness: an SPRT-positive commit that quietly changes the value of the tree would pass. It does not wait for the arena, because unlike the floor experiment it makes no strength claim — flips never; the oracle is deleted only if the search stops being able to answer a full-width question, which is the same day perft stops being enumerable.

D-107: Amends D-80's field list: the returned `SearchInfo` carries the whole search's `seldepth_turns` as well as its nodes, time and table occupancy, and only the line, the score and the depth come from the last completed iteration. D-80 enumerates both sets and puts `seldepth` in neither, while `search.rs` overwrites it with the run's maximum and `info.rs` documents that it does — so the code is self-consistent and the ADR is the stale one, which rule 10 says to amend rather than leave. It is protocol-visible: an aborted iteration that went deeper than the last completed one prints a totals line whose `seldepth` exceeds its `depth_turns` (measured: `depth_turns 2` beside `seldepth 3`). That is the honest reading — the selective depth is a statement about work done, which is what the totals line reports, and D-94 already made `seldepth` count a win the search reached rather than a depth it completed — flips never; a consumer wanting the completed iteration's selective depth is asking for a field that does not exist yet, which would be additive.

D-108: Amends D-70's naming of its own guarantee, and resolves its contradiction with D-61. D-70 offers the per-ply recompute playout as "the real guarantee" behind two opportunistic desync checks; it is not, and cannot be — that test hands `Eval::apply`/`undo` the same `(cell, mover)` pair it hands `GameState`, so it is a statement about the update ARITHMETIC and structurally cannot see a caller that mispairs. Measured, both checks are escapable: an `undo` of a cell never applied, where every window through it already holds a stone of that player, drifts the carried sum silently (12 stones, true value 3036, 1248 after), and a double `apply` in a sparse region does the same, because the guards fire only on an absent entry, a zero count, or a window already full. What actually prevents mispairing is `Position`'s apply/undo discipline in pistol-search — one crate up, unnamed by D-70. Relatedly, D-61 justifies passing the player in on the grounds that an implementation "can cross-check what it is told", which the one implementation cannot and which D-70 says it deliberately never will; the honest reason for the argument is that a backend needs no board reference, and the cross-check clause is withdrawn. So the guarantee is stated where it lives: the eval's own checks are opportunistic and named as such, and pairing is the caller's invariant — flips if a second apply/undo pairing path appears, which Stage 1's threat-first generation is a candidate to create (D-79), and which would buy the occupancy mirror D-70 prices.

D-109: Amends what D-89 records about `configs/gate_v0.toml`: it narrows TWO things against the deployment config, the candidate radius AND the transposition table (16 MiB against 256 MiB), and its own comment claimed it narrowed "what the search looks at, and nothing else". The second one is not a memory knob, because move choice is not invariant under table size: two turns that tie on score are separated by whichever the ordering tried first, the table's move goes first, so table contents decide the tie — measured, the same position at depth 3 returns `-1,0/0,3` at 1 MiB and `-1,0/0,-1` at a degenerate table, both scoring `MateIn(3)`, and the reported line differs on another position. This breaches no rule 4 (a different `tt_bytes` is a different config, and each is deterministic in itself) and it invalidates nothing the gate certifies, because what the gate certifies is that two runs of the SAME config agree — but it does mean a bestmove observed under this document is not a bestmove the deployment config would make, and D-89 recorded only the radius as the reason for that. The table stays small: two processes run at once in the determinism gate and a table that fits in cache keeps the gate about the search. What changes is that the document now says so — flips if the gate ever has to certify a move rather than a reproduction, which would need the deployment config and the time to run it.

---

D-110: Pre-approves a fourth `Eval` method, `delta(cell, player) -> i32` — the score change of placing a stone WITHOUT mutating state — as a *provided* trait method whose default body is the apply/value/undo roundtrip D-76 already performs, amending D-61's "exactly three methods": a provided method with that default cannot be a correctness fork for a backend that does not override it, because such a backend computes precisely what the search computes today, and object safety (which D-61 chose deliberately) is unaffected by a method taking `&self` and returning `i32`. For a backend that DOES override it — which is the whole point, `HandcraftedV0` first and the Stage-2 codebook net after — the equivalence stops being structural and becomes a claim, so an override ships with a test asserting `delta(c, p) == apply(c, p); value(p); undo(c, p)` over the same seeded playouts D-68 already runs the eval against; that is rule 7's oracle discipline and D-68's, applied to the one place this method can silently disagree with itself. It is approved and deliberately NOT implemented here: D-76 registers the roundtrip as a hotspot on reasoning rather than on a profile, and CLAUDE.md rule 5 wants a measured hotspot before a perf structure exists at all — which is D-59's argument for declining the key cache, applied to the eval. So the fast path and the switch in `ordering.rs` land only after the flamegraph session (D-114) confirms H1, and this line exists so the shape is settled before the profile rather than designed under it — flips if that profile refutes the hotspot, in which case the method is never added and its absence is this line's own answer.

D-111: No static evaluation is returned as a NODE'S ANSWER half way through a turn: a search horizon that would land at phase 1 extends one ply to complete the turn — a mover who still owes a stone has not finished doing anything, so the pattern tables would be scoring a position no player is ever handed, and the number would be attributed to a side half a move before it is that side's to have. Binding on Stage-1 quiescence design (a threat-only quiescence stands pat and extends in TURNS, never in plies). The wording is deliberately narrower than "static eval is consulted only at a turn boundary", which is measurably FALSE of this code: `Eval::value` is read at phase-1 nodes twice over, and neither read is an answer. (1) Move ordering reads it through `Position::static_score_after` at every node that has candidates, phase-1 included, to score a HYPOTHETICAL stone it takes straight back off (D-76) — a comparison key among siblings, never a value handed to a parent, which is why a saturated band there degrades ordering rather than corrupting a score (D-105). (2) The table's `static_eval` field is filled from `Position::value()` at every stored node, phase-1 included — bookkeeping, and nothing in Stage 0 reads the field back (`tt/entry.rs` packs and unpacks it; no search consumer exists). Both were measured rather than assumed: an assertion placed inside `Position::value()` fires on the first search, from the store site, at ply 1, and one placed inside `static_score_after` fires from ordering at ply 1. So the assertion sits at the two places that DO answer — a `debug_assert!` naming `STATIC_EVAL_MID_TURN` at the depth-0 horizon in `pvs.rs`, debug-only because it sits at every leaf and `cargo test` is the gate that reads it, plus the empty-candidate horizon, which already carried the same statement as a release-active `assert!` under `NO_CANDIDATES_MID_TURN` (D-104). The search satisfies this by construction and not by luck: `plies_for` sums the stones each turn ahead owes, so a depth budget stated in turns runs out exactly where a turn does, with rule 3's one-stone first turn and rule 4's truncation both inside that sum. Three behaviour-named tests drive it — over a search-chosen playout from turn 1, a threat-dense midgame root and a quiet position — each counting the horizons it actually reached and refusing to pass on a floor, because "nothing panicked" is also what a search that did no work reports (D-90's positive-content argument). The trap the two non-answer reads leave is named here so a later stage does not walk into it: futility and razoring are the readers `static_eval` exists for (Stage 4's PROTOTYPE gauntlet), and a pruner reading a phase-1 entry would import exactly the mid-turn value this line forbids; D-114's H1 profile will likewise show `Eval::value` called at phase-1 nodes, and that is ordering doing its job rather than a breach — flips never for the invariant; the `static_eval` exception closes when a consumer for that field lands, which must either skip phase-1 entries or stop storing them, and amends this line when it does.

D-112: Parallel search (Stage 4, Lazy SMP) constructs ONE eval instance per thread from immutably shared weights — a factory or constructor path — and `Clone`/`Send` bounds on the `Eval` trait itself are rejected. An eval's state is per-position bookkeeping that the searching thread mutates on every ply (D-62's window map), so it is precisely the thing that must not be shared; the weights are the thing that must be, and they are already read-only after load (D-64). A `Clone` bound would break the object safety D-61 chose deliberately, and a `Send` bound on the trait would advertise a thread-safety property of the wrong object — the instance is thread-*confined*, not thread-safe, and a bound saying otherwise invites exactly the sharing this forbids. Decided now rather than at Stage 4 because the seam it constrains is how a backend is BUILT, and Stage 2's codebook net lands first: a net constructible only by cloning a warmed instance would be discovered as a design error a whole stage late — flips if a backend appears whose per-position state is genuinely immutable, which would still get its own instance per thread and simply not care.

D-113: A candidate radius of 9 through 64 is provably equivalent to 8, so running a radius experiment above 8 is an experiment-design error and not a config error — the candidate set is the policy's ball INTERSECTED with pistol-core's own legality answer, asked one cell at a time (D-77), and the rule-5 legal region is the union of radius-8 balls around the stones (rule 5, D-101); a policy ball of radius at least 8 centred on a stone therefore already contains that stone's whole legal ball, and the intersection discards everything beyond it. Every radius in 9..64 yields the identical candidate set, the identical tree and the identical move, and `MAX_CANDIDATE_RADIUS` stays what D-18 calls a rejection bound — a statement about what a document may say, not about what plays differently. Two things are deliberately NOT done. No validation change refusing radius > 8: that check would have to import the rules' constant into config validation, which is the conflation CLAUDE.md rule 2 names outright and D-20 exists to prevent, and the cost of the error it would catch is a wasted experiment rather than a wrong move. No enumeration of the equivalent range in the schema either: the radius policy is scheduled for supersession by Stage 1's threat-first staged generator (D-52, D-79, ROADMAP WP-1.5), and pinning a table against a policy with a known expiry is work that expires with it — flips if the radius policy outlives Stage 1 as the primary candidate source, at which point the equivalence is worth one comment in the config document itself.

D-114: The flamegraph session is pre-registered with exactly two hypotheses and adjudicates BEFORE any fix (CLAUDE.md rule 5's no-post-hoc-thresholds, applied to profiling): H1 = the eval apply/undo pair per candidate per node in move ordering; H2 = per-node allocation in `candidates.rs`/`ordering.rs`, each of which builds a fresh `Vec` at every node. This amends D-76's letter: that line calls the roundtrip "this work package's registered hotspot and the first thing a Stage-1 bench should attack", which registered it on reasoning alone and left no room for a competing explanation of the same cost — H1 is that same suspicion, now stated as a hypothesis a profile can refute rather than a conclusion a bench merely quantifies. The profile is operator hardware, a release build, instrument mode, and fixed-NODE runs at two stone counts — nodes and not movetime, because D-95 makes a movetime budget a floor plus one uninterruptible first iteration and would profile that instead of the search; two stone counts, because D-95's own measurement shows candidate-set cost grows with how far apart the stones sit, so a single opening profile would understate H2 by construction. Whatever the profile confirms then earns its own rule-5 pre-registered bench with a hotspot, an expected gain bracket and an abort threshold: the profile chooses the target and the bench judges the fix, and neither is allowed to do the other's job. The session runs on the operator's machine as ROADMAP WP-1.3 — flips never; a third hypothesis means a new line here and a new profile, never an addition to this one after the numbers are in.

D-115: In-source `#[cfg(test)] mod tests` blocks are PERMITTED, for pinning private invariant guards only — behaviour-level pinning stays in the behaviour-named integration suites under `tests/` (CLAUDE.md rule 7), and no item is made public, no `pub(crate)` is widened and no signature is altered to let a test reach it. The gap this closes is real: a guard whose removal is invisible from outside the crate cannot be pinned from outside the crate, and the only alternative on offer was to widen the surface, which trades a permanent API for a test. Two such guards land with this line. `ordering::tests::a_table_move_that_is_not_a_candidate_is_dropped_rather_than_played`: a 64-bit verification word can in principle answer about a different position (D-8), so the table's move is not necessarily a candidate here, and promoting one anyway would place a stone the policy never offered — surfacing a level down as `CANDIDATE_ILLEGAL`, naming the policy for something the ordering did. `pv::tests::a_cleared_line_is_not_adopted_by_the_parent_that_promotes_over_it`: `clear` drops the length and leaves the coordinates where they are, so the length is the entire guard against a parent adopting a previous sibling's line and reporting a variation it never searched. Each was mutation-checked — the guard removed, the test observed red, the guard restored — and each names the mutation it was checked against in its own doc comment, because a guard test never seen to fail pins nothing, which is D-55's and D-60's argument about a value copied out of the code it checks — flips if an in-source module starts pinning behaviour rather than an invariant, which is evidence the behaviour has no home in `tests/` and the crate seam is in the wrong place.

D-116: A reviewer finding is verified with a minimal reproducer before its fix lands, and a finding that cannot be reproduced is recorded as REJECTED together with the reproducer that was attempted — codifying the R3 episode. A review is worth having precisely because it is written without the implementer's assumptions, which is the same reason a review can be confidently wrong about a path it never ran; a reproducer is the cheapest thing that separates the two cases, and writing one is bounded work where chasing a speculative fix is not. The rejected half matters as much as the verified half, and therefore needs somewhere durable to live: a rejection that would change a reader's model of the system goes in THIS file as its own line, and the rest stays in the work package's review record — because a rejection recorded only in an ephemeral review report does not reach the next reviewer, which is the whole failure it exists to prevent. D-101 is the precedent and the shape: a question asked, answered against the platform spec, and written down here with what a demotion would have cost, so nobody re-derives it. The amendment is one sentence in CLAUDE.md's Process section, beside D-99's — flips never; a finding whose reproducer would itself be a work package is still recorded, as a finding with its reproducer named rather than written.

D-117: The Stage-1 work-package cut of record is WP-1.1 search oracle -> WP-1.2 minimal arena -> WP-1.3 calibration experiments -> WP-1.4 movetime-ceiling fix -> WP-1.5 threat infrastructure -> WP-1.6 threat-only zone-bounded quiescence -> WP-1.7 killers/history/countermove on pair moves -> WP-1.8 AND-OR solver and relevance-zone Deep df-pn (+1+epsilon, GHI), written out in docs/ROADMAP.md's Stage-1 section, whose previous ordering this supersedes. Two things moved and both for the same reason. The differential search oracle (D-106) goes FIRST, ahead of the arena: it makes no strength claim and so needs no judge, while everything after it is a pruning change that SPRT judges for strength and not for soundness — a pruning commit that quietly changed the value of the tree would pass an SPRT, so the soundness reference has to predate the first pruner rather than merely predate the threat machinery. The arena lands second and is what every WP after it is measured by (CLAUDE.md rule 6). The rest is dependency order made explicit: calibration (r=2 vs r=3 fixed-node paired SPRT with its verdict pre-registered, the decided-window floor experiment D-105 keeping its pre-registered home there because it is an arena experiment too, and D-114's profile) needs the arena to exist; the movetime-ceiling fix (D-95) is play-mode only and blocks nothing, so it sits ahead of the threat work rather than behind it, where a forfeit risk should not wait; threat infrastructure — window-count and hot-window tables, the must-block filter, the unblockable-double-threat check, the mechanisms docs/research/sealbot_notes.md measures working in another engine for this exact game — is the data structure quiescence needs and therefore precedes it; quiescence is bound by D-111; and killers, history and countermove are ordering changes worth measuring only once the candidate set they order is the threat-first one. The countermove is named explicitly because the research report's Stage-1 line and `ordering.rs`'s own module doc both promise it, and a compressed restatement that dropped it would have been the silent drift rule 10 forbids. Each WP gets its own prompt and names its gates there — flips per WP: a cut that moves is an amendment on this line, never a silent reorder of the ROADMAP.

D-118: `crates/pistol-search/src/pvs.rs` is the first file to exceed CLAUDE.md rule 9's soft cap; it carries its why-justification in its own module doc (stating no count, as the rule requires), and the file-justification GATE stays unmechanized. This is recorded here because the choice was otherwise stated only in a `tools/ci.sh` echo — which is the silent architecture drift rule 10 forbids, and which that echo itself said needed a line here. The justification: `visit` is one recursion whose parts are not independent — the window a child inherits depends on whether the ply was the mover's second stone or the opponent's reply, the table's bound depends on the alpha the node started with, the abort path must unwind through every one of them without a result being used, and the invariants that make it correct (the mate re-basing D-72 fixes, D-78's no-cutoff-at-a-PV-node rule, the two horizons of D-111) are stated against the whole. Splitting it would pass six values between free functions and put the reasoning in a different file from the code it constrains, which is a worse read for a smaller number. The honest reduction is scheduled rather than forced: Stage 1 moves candidate generation out of this file entirely (D-117, WP-1.5). The gate is not mechanized because a check has to RECOGNIZE a justification, which means a marker convention that every future justified file must carry, and inventing a workspace-wide convention to police one file is a schema decision taken at the worst moment to take it; `tools/ci.sh` therefore reports the gate as OWED rather than as having nothing to run, which is the honest state rather than the skip-with-default rule 3 forbids — flips when a second file crosses the cap, which is when the convention is worth designing and is what this line is waiting for.

---

D-119: What the differential search oracle asserts, in three parts, because "the search agrees with the reference" is three different claims. (1) About the MOVE, only that it is in the reference's ARGMAX SET — never identity. Among equal-scoring turns the engine's pick IS its move-ordering pass, which the reference deliberately does not have, so pinning the move would pin the ordering rather than the value, and a Stage-4 retune would read as a soundness failure; WHICH tie the engine takes is pinned already, by the cross-process determinism gate (D-7), and ties are the normal case rather than the exception. (2) About the SCORE, that it agrees once CLASSIFIED, with the reference carrying its own three-band `WinInTurns`/`Value`/`LossInTurns` built from turn arithmetic alone and the test mapping `classify`'s answer onto it — a reference that built its mate scores with the crate's own `mate_in` would cancel an error in `MATE`, in `mate_in` or in `classify` against itself, and exact mate distances are precisely what the work package asks this oracle to certify, so D-3's packing has to be under test rather than assumed. (3) About the LINE, that a claimed mate actually arrives — on both sides of the band, a claimed win and a claimed loss alike, by the side the score says wins and on the turn the distance names — replayed through `GameState::make_turn` and not by placing a canonical pair in canonical order, which D-51 says is not always the playable ordering. The third part exists because (1) and (2) look only at the ROOT turn, so a principal variation whose tail is not the line the score was proved on passes both, which is what D-78 exists to prevent and what the determinism gate cannot see because it compares two runs of the same engine. The expensive half of that — every prefix position's reference value under D-72's parity — is deliberately not bought — flips if the reference ever gains an ordering pass, at which point move identity becomes assertable and would say something.

D-120: The oracle's depth ceiling and runtime budget, measured before the matrix was chosen rather than discovered after (CLAUDE.md rule 5). Full-width per-TURN branching in this game is about C-squared where C is the candidate count, and C is at least 6 in the smallest position that exists (one stone, radius 1): measured on that position at radius 1, a reference run costs 49 nodes at depth 1, 5 377 at depth 2, 999 841 at depth 3 (2.3 s release) and 273 594 001 at depth 4 (614 s, measured and not extrapolated). So the suite splits in two, the way `perft_tests.rs` splits for the same reason (D-54): `search_oracle_tests.rs` carries what a debug build can afford on every commit, against a pre-registered ceiling of 5 s debug on the development machine (measured: 3.95 s and 1.85 s for its two binaries, which grew the workspace test run from 83 s to 89 s), and `search_oracle_deep_tests.rs` carries the third turn, is `#[ignore]`d, and is run in release by `tools/search_oracle_check.sh` as CI gate 10 (measured: 84.4 s, with the whole gate 86 s). Depth 4 is NOT run at any radius, and that is the number above rather than a judgement. Two consequences are recorded because they are not obvious. The exact mate distance of three costs 37 050 968 reference nodes (84 s) on a purpose-built eleven-stone position, and that is a floor rather than a fixture choice: a forced mate in three needs a double four after the mover's turn, which needs seven own stones and therefore five before it, which with P1's always-odd stone count fixes the position at eleven stones, and fifteen candidates at radius 1 is the packing floor for eleven stones. And the reference enumerates BOTH orderings of every pair at every interior node, as the engine's ply-level recursion does (D-79); evaluating each unordered turn once instead is exact and measured 3.9x cheaper at depth 3 on the opening (999 841 nodes against 255 418) and 6.75x at depth 4 (273 594 001 against 40 535 269), which would have brought the mate-in-three case to roughly fifteen seconds and made depth 4 affordable on one position — and it is declined, because it would change a method the work package pre-registered in order to hit a budget, because the dedupe key has to carry rule 4's truncation (the pair {a,b} whose smaller cell wins is realised by one ordering only) and the reference is the one component here that must have no correctness argument of its own, and because the two orderings agreeing is D-79's premise and one of the three arguments D-106 built this oracle to certify — flips if the release gate's runtime becomes the thing that stops it being run, in which case the dedupe is the change to make and this line records what it costs and what it gives up.

D-121: The table-size independence check asserts that the SCORE does not move and that every size still plays a turn the reference rates best; it does NOT assert that the MOVE is the same. D-109 measured that move choice is not invariant under table size — two turns that tie on score are separated by whichever the ordering tried first, the table's move goes first, so table contents decide the tie — so a test pinning move identity would pin a property this project has already recorded as false, and would be a time bomb rather than a gate. Pinning it "only where it empirically holds" would be worse: that is a threshold chosen after the numbers are in, which the Process section forbids outright. The sizes run are 96 bytes, 1 MiB and 256 MiB — the first being the smallest table the type can build rather than the smallest a config may state (D-19's `MIN_TT_BYTES` binds documents, not `Table::new`), because one bucket is maximum collision pressure and that is where a 64-bit verification word (D-8) would show, and it is what D-109's own measurement used. What the one-bucket size deliberately does NOT cover, because an earlier draft of this line claimed it did and both reviews disproved it with the same reproducer: the table's mate re-basing (D-72). Replacing `score::shift` with the identity leaves every one of the oracle's assertions green at every table size and every depth, and is caught immediately by `tt_tests::tt_mate_score_depth_adjustment_roundtrips`. The reason is structural and no fixture or size fixes it — within one search from one root, `turns_from_root` is a function of the key (the stone count fixes the turn, the phase and the mover together, D-58), so every probe re-bases by exactly the amount it was stored with and the conversion is provably a no-op; the re-basing only matters for a table that survives into a search from a DIFFERENT root, and this oracle builds a fresh `Searcher` per run precisely so that a carried table is not an input to the answer (D-109). That property is real and it is pinned in `tt_tests`, one crate-local unit away, not here — flips never; the score not depending on the table is the soundness claim this oracle exists to make, and the move not depending on it is not a claim anyone made.

D-122: The oracle reads `crates/pistol-cli/tests/fixtures/tactical_v0.txt` and `crates/pistol-core/tests/fixtures/perft_positions_v1.txt` in place, from pistol-search's test tree, and does NOT re-pin their SHA-256s. D-37 pins a fixture in the test that reads it because a fixture carries GOLDEN VALUES, and editing the file without editing the expectation must be a red test; this suite carries no golden values at all — its expectation is agreement between two implementations, which is invariant under every edit to the position set — so a third std-only SHA-256 in the workspace would pin a file whose contents cannot change this suite's verdict, and drift is still caught by the owning crates' own pins in the same `cargo test --workspace` run. What this does create is an undeclared cross-crate coupling: moving or renaming either fixture breaks a test in a crate that does not own it, with no compile-time signal, and the strict readers in `tests/common/fixture_text.rs` know both grammars, so a new directive in either file is a loud failure here as well as there. That is the cost, it is named rather than argued away, and it is cheaper than either a third hashing copy or a ninth crate to hold one — flips if a third crate needs to read the same fixtures, at which point the loaders and the pin earn a home together rather than a third copy each.

D-123: The oracle compares the engine's answer at the depth its iterative deepening STOPPED at against the reference's answer at the depth that was REQUESTED, and that is sound only while the search is full-width. `search.rs` breaks out of the deepening loop on `is_mate(score)`, so a position with a mate in one is answered from the first iteration however deep the budget was; the comparison still holds because in a full-width search a mate score at the root is EXACT — a proven win ends the game, a proven loss requires every root turn to be one, and any shorter mate would already have been inside the horizon that found the longer one, so neither the fact nor the distance moves with more depth. The three mate-in-one cases kept at depth 2 in the release tier are there for exactly this: they are the only runs where the engine answers from a depth other than the one the reference searched, and their agreement is what makes every other depth comparison in the suite mean what it says. This is written down rather than left in the argument because its premise is the thing Stage 1 exists to remove: the first forward-pruning or reducing change makes "mate found at depth d" no longer exact, and the oracle must then either compare at the depth the engine reports or have this argument made again — flips with WP-1.5 and whatever it prunes, which is the same day this line becomes the reason a comparison basis has to change.

D-124: A value-differential oracle cannot see a search that narrows its candidate set at the TAIL, and that is a limit of the method rather than of this matrix. Verified with a reproducer before being written down (D-116): dropping the worst-ordered candidate at every node of `pvs::visit` — one `cells.pop()` after `order` — leaves every one of the oracle's assertions green, all ten always-on tests and all three release ones, across 31 fixtures, three depths, two radii and three table sizes; dropping the BEST-ordered candidate instead turns six of the ten red at once, naming the fixture and the move, and an off-by-one in the engine's mate distance turns two red. The reason is structural and no fixture fixes it: the oracle compares VALUES, the dropped turn is the one move ordering rated worst, and a turn that is never the argmax cannot move a maximum however deep it is searched — a wider fixture set or a fourth turn would not help, because the mutation is invisible by construction. What would help is comparing the engine's per-node candidate set with the reference's, which needs visibility into `pvs` that this work package's non-goals forbid (no new public surface, and D-115 permits an in-source test only for a private invariant, not for a cross-crate comparison). This is recorded because Stage 1's very next candidate-set change is exactly this shape: WP-1.5's threat-first staged generation supersedes the radius policy with DOMINANCE PRUNING, which is a rule for dropping moves it rates worse (D-52, D-79). So the work package that lands it inherits the fact that this oracle will not judge its dominance rule, and owes a check of its own — flips when that check lands, which is where the visibility question gets settled.

D-125: The oracle's soundness claim about the SEARCH WINDOW rests on exactly one comparison, and it is named here so that nobody cheapens the gate by dropping it. Found independently by both reviews, each with its own reproducer: deleting the re-search in `pvs::child` — so that a null-window scan which fails high is returned as the node's value — and making the transposition cutoff unconditional — any bound, any depth, principal-variation nodes included, in breach of D-78 — are each invisible to EVERY always-on assertion, over 31 fixtures at two radii, three table sizes and two turns, and invisible as well to the release tier's depth-2 breadth over eighteen fixtures and to the 37-million-node mate-in-three case. The one comparison that catches either is `tight_cluster_at_a_turn_boundary` at depth 3 and radius 1, 9 308 846 reference nodes and about 21 s: engine `Value(780)` against reference `Value(910)` for the deleted re-search, and `Value(332)` against `Value(910)` for the unconditional cutoff. That is structural rather than a fixture gap — at two turns the sub-search below a null-window scan is shallow enough that the window returns the exact value, so no width, radius or table size at depth 2 can see a window bug. Two things follow. The gate's cost is not where its power is: about 84 of its 85 seconds are the mate-in-three case, which buys the only exact mate distance above two and catches NEITHER window bug, while the 21 s case carries the window claim alone — so a future trim of the gate must know which second is which. And every commit between gate runs is covered by a tier that cannot see a window bug at all, which is a consequence of the debug budget (D-120) rather than of the method: unlike D-124's blindness, a depth-3 comparison does close this one — flips if a depth-3 comparison ever becomes affordable in a debug build, which is the day the always-on tier can carry this claim itself.

D-126: Takes D-120's own flip clause, operator-approved: the reference values each unordered turn ONCE, and both-orderings enumeration survives as a mode with exactly one caller. D-120 declined the dedupe on three grounds and two of them are now paid for rather than argued away. The dedupe key is the `Turn` and not the cell set, so rule 4's truncation is carried and not merged — a first stone that completes a line makes a ONE-stone turn, enumerates no pair, and therefore never suppresses the other ordering of a pair it belongs to, which is the one ordering that pair has; the skip test is membership in a per-node ledger of first stones whose ply CONTINUED the turn, and the argument that this is exact in both directions is stated against the ledger in `tests/common/pair_dedupe.rs`. D-79's premise — the two orderings of a pair are worth the same at search depth — was the third ground and it keeps a check rather than losing one: `reference_dedupe_matches_both_orderings_enumeration` runs both modes over four fixtures at the depths a debug build affords and compares the whole per-turn map, `reference_dedupe_matches_both_orderings_below_the_root` does the same at two turns in the release tier where rule 4 truncates at an INTERIOR node, and `dedupe_keeps_the_pair_whose_ordering_rule_4_truncates` asserts the shape directly so that two modes agreeing by both losing a turn is still red. Verified by mutation in a separate worktree, never the live tree, before this line was written (D-116): keying the dedupe on the cell set instead of the turn turns the first and third red, dedupe by ascending order alone without the membership test turns those two red plus the movegen cross-check, and reversing the interior candidate loop fires `REFERENCE_DEDUPE_KEY_UNSORTED` by name. The re-measured numbers, which supersede D-120's: tier-1 1.74 s and 1.50 s debug against a 5 s budget (was 3.95 s and 1.85 s), the workspace test run 86.6 s (was 89 s), the release gate 19.6 s (was 86 s), its deep tier 18.1 s (was 84.4 s), the mate-in-three case at depth 3 6 714 383 reference nodes and 18.0 s run alone (was 37 050 968 and 84 s), and `tight_cluster_at_a_turn_boundary` at depth 3 — the one comparison that carries D-125's window claim — 1 879 674 nodes (was 9 308 846). The gate-shape question D-120 left open is closed by the pre-registered rule the operator set before the measurement: at 18.0 s the mate-in-three case is under the 35 s that would have re-opened the cut, so the gate keeps its shape. And a fourth turn is now affordable and is added, on the cheapest position that can carry one: `empty_board_turn_1` at depth 4 is 255 419 reference nodes and 1.4 s, because an empty board offers the policy one cell and the tree below the opening stone is `one_stone_at_the_origin`'s depth-3 tree — what it buys is on the ENGINE's side, seven plies of `plies_for` over rule 3's one-stone opening turn plus three pair turns, one turn past anything else in the suite. The next cheapest depth-4 case is that position's own at 40 535 269 deduped nodes, which is minutes, and it stays out — flips if a reference-side bug is ever traced to the dedupe, in which case `PairOrder::BothOrderings` is one argument away and the mode comparison is what would have caught it.

D-127: `[profile.release] overflow-checks = true`, operator ruling, amending D-14's letter and not its reason. D-14 defers `[profile.release]` settings because codegen tuning is a perf-sensitive change and CLAUDE.md rule 5 wants a pre-registered hotspot and an IQR-gated bench before one lands; this is not codegen tuning, it is which CHECKS the shipping build keeps, and the thing it prevents is the one class of error a rule-3 fail-loud project cannot otherwise catch — a wrapped i16 coordinate or a wrapped turn count is not a crash, it is a legal-looking wrong answer the engine goes on to play, and in release Rust wraps it silently. D-34 and D-44 already require checked arithmetic wherever a coordinate is computed by hand; this closes the same hole for arithmetic nobody wrote a `checked_*` for, including whatever a future contributor writes. Its cost was measured rather than assumed, because rule 5's spirit applies even where its bench harness does not exist yet (`bench` is still unimplemented, D-14): fixed-NODE runs of 2 000 896 nodes under `configs/instrument_v0.toml`, five repetitions each, median nps 206 925 -> 204 325 on the one-stone opening and 167 178 -> 165 276 on the quiet midgame position — 1.3% and 1.1%, against a 0.5 s design point that is nowhere near tight enough for that to buy anything. It is pinned by `bare_integer_overflow_panics_under_this_profile`, which runs in the release pass of `tools/search_oracle_check.sh` because a debug build has overflow checks by default and would pass the test without asserting anything; verified by mutation in a separate worktree before this line was written (D-116), where deleting the flag turns that test red in release and nothing else in the workspace notices — flips if a profile shows the check inside a hot loop that a `checked_*` with a named panic would state better, which is a code change and not a profile change.

D-128: `[profile.release-checked]`, inheriting release and adding `debug-assertions = true`, operator ruling. It is the profile red-team and deep-verification runs build, and it exists so that nobody edits an assertion's CLASS to go looking for something: the alternative that was actually happening is hand-hardening a `debug_assert!` into an `assert!` for one investigation and hand-reverting it after, which is a source edit whose only record is that somebody remembered to undo it. Measured shape of the three profiles, so the claim is a run and not a reading of `Cargo.toml`: dev has debug assertions on and overflow checks on, release has debug assertions off and overflow checks on (D-127), release-checked has both on at release optimization. Nothing in CI runs it, deliberately — it is an instrument, not a gate, and a gate that ran it would double every release gate's build for checks the deployment build does not keep — flips if a `debug_assert!` ever guards something a gate must see, which would mean it was misclassified under D-129 rather than that this profile needs a gate.

D-129: The assertion taxonomy, operator ruling, stated so that the choice between the two macros stops being a judgement call made twice. `assert!` = a CORRECTNESS invariant, active in every profile: something whose violation makes the answer wrong rather than merely suspicious — transposition-table field ranges, the desync guards in `Position`, `GameState` and `pv`, coordinate overflow, and a candidate policy that runs dry half way through a turn. `debug_assert!` = a DIAGNOSTIC whose cost is unwanted on the deployment hot path: a statement that is true by construction and is asserted because a future extension could break the construction, sitting somewhere that runs per node or per leaf — `STATIC_EVAL_MID_TURN` at the depth-0 horizon is the case the rule was written from (D-111), and D-128's profile is where it is live. A compile-time `const` assertion is outside the taxonomy entirely: it is a build error, not a runtime class. The existing assertions were audited against this and NOT ONE was reclassified, which is the honest outcome and is recorded because a taxonomy that changed nothing looks unmotivated otherwise: the workspace has exactly one `debug_assert!` and it is the exemplar the definition names, and every other runtime assertion guards a wrong answer rather than a suspicion — flips if a correctness invariant is ever measured to cost enough to matter, which is a rule-5 bench and a named exception on this line, never a quiet demotion.

D-130: A review is dispatched against a NAMED REVISION and mutation testing runs in a separate git WORKTREE, never the live tree — operator ruling, amending CLAUDE.md's Process section beside D-99's and D-116's sentences. Both halves close a way for a review to be about nothing. A reviewer reading "the working tree" reads whatever the implementing session happens to have saved at the moment it looks, so a finding can name a line that no longer exists and a clean verdict can certify code that was replaced while it was being read; naming the revision costs one `git stash create` where the work is uncommitted, and a report header that states the revision and whether HEAD still matches it makes a stale review say so instead of looking current. Mutation testing has the sharper failure: a mutation is a deliberate break, and a deliberate break sitting in the tree the implementer is editing is indistinguishable from a regression — worse, an interrupted mutation run leaves it there, and the next gate to go red is diagnosed against a file nobody meant to change. A worktree is the cheapest isolation `git` already has, it shares the object store so it costs a checkout rather than a clone, and `git worktree remove --force` is one command. This session applied the rule to itself: D-126's dedupe and D-127's profile flag were both mutation-verified in worktrees at named revisions, and the REVIEW-impl over them was dispatched against a stash-create SHA — flips never; a harness that could pin a revision another way is this rule under a different name.

D-131: CLAUDE.md rule 9's file-justification gate is mechanized as `tools/file_justification_check.sh`, CI gate 11, taking D-118's flip clause — a second file crossed the cap, which is what that line said it was waiting for. The convention D-118 declined to invent, now that there is more than one file to police: the cap is **300 lines**, which is what "~300-line soft cap" is worth as a number a script can compare; over it is not a failure but a demand for a sentence; and the sentence is a comment line carrying `RULE9-JUSTIFICATION:` followed by the why on that same line. The marker goes in the module doc where the argument already lives rather than in a pragma, because a checker has to RECOGNIZE a justification and prose cannot be recognized, and because the reader who needs the why is reading the top of the file. Rule 9's other half — counts are derived, never asserted — is checked too: a why with a numeral beside "line" or "lines" fails. That check catches the literal form and not a count spelled out in words, and the script says so about itself rather than implying a proof. The gate self-tests before it runs, over five seeded files in a temporary directory, one per verdict: a checker nobody has watched fail is not a checker, and this one is mostly asked to say "no" about files that do not exist yet. Landing it found the second violation immediately and it was this session's own: `search_oracle_universe_tests.rs` had grown to 327 lines under D-126, and the honest answer there was a split rather than a sentence — the dedupe equivalence tests are the reference checking ITSELF, which is neither an agreement assertion nor a universe one, so they moved to `search_oracle_dedupe_tests.rs` and the gate script runs the third binary. `pvs.rs` keeps its argument unchanged and gains the marker on its heading — flips if a third file needs a justification whose why does not fit one line, which would make the marker a block convention rather than a line one, and is a change to the checker rather than to the rule.

D-132: Amends D-118's second half, which said the file-justification gate stays unmechanized and that `tools/ci.sh` would report it as OWED. It is mechanized (D-131) and the OWED line is deleted from `ci.sh`, which is the point of having written the deferral down: D-118 named its own flip condition — a second file crossing the cap — and that is what happened, in the very next work package, for the reason D-118 anticipated (a convention is worth designing when there is more than one case to design it for). D-118's FIRST half is untouched and still current: `pvs.rs` is over the cap, its why is the one that line records, and the honest reduction is still scheduled for WP-1.5 — flips never; this is a deferral ending, not a decision reversing.

D-133: Amends D-117's cut in one place and nowhere else: WP-1.2 splits into **WP-1.2a** opening corpus extraction and fixture curation, then **WP-1.2b** minimal pistol-arena. Order is unchanged, everything from WP-1.3 onward keeps its number, and D-117's argument for why the arena sits second is untouched. The reason for the cut is what the two halves are made of: 1.2a is data work — choosing paired openings, balancing them, and pinning them into the sha-indexed fixture form a runner reads — with no runner in it, and 1.2b is a match runner, an SPRT, a dedupe and a compute ledger with no judgement about positions in it. A single package holding both lands neither under review, because a reviewer attacking the corpus and a reviewer attacking the harness are reading for different failures and the one report would carry both badly. The split also puts the corpus where the WPs after it can see it: WP-1.3's calibration experiments are paired-opening SPRTs (D-117), so the openings are their input, and a corpus that arrives inside the runner's own package is a corpus nobody reviewed on its own terms. No renumbering, deliberately: D-117's numbers are cited from ROADMAP.md, from work-package prompts and from this file, and a renumber to close a letter gap would invalidate every one of those citations to buy nothing — flips per WP, as D-117 says: a cut that moves is an amendment, never a silent reorder.

D-134: Amends what D-126 claims about coverage, on a REVIEW-impl finding with its reproducer. D-126 says D-79's premise "keeps a check rather than losing one", which is true in KIND and not in coverage, and the difference is large enough that a reader who took the sentence at face value would over-rate the suite. Before: `record`'s `REFERENCE_PAIR_ORDER_DISAGREES` compared the two orderings of every pair at the root of EVERY reference run — 31 fixtures, three depths, two radii, both tiers, on every commit and every gate. After: `PairOrder::BothOrderings` has two callers, five distinct fixtures between them, radius 1 only, two turns deep. D-126 states the new scope, so the narrowing was derivable, but it never states the old one, and "the check moved" reads as "the check is the same size". What is bought back is a different shape rather than the same shape smaller: the mode comparison compares whole per-turn MAPS instead of catching a disagreement at the node that reached it, which is why the reviewer's own cross-check — the same instrumented walk over 12 272 distinct node positions, plus the deduped root turn set against `generate_turns` at `LEGAL_RADIUS` on two positions where rule 4 actually truncates, 78 072 and 67 288 turns, no turn missing or extra — found the dedupe exact where a value comparison alone would not have proved it. The narrowing is now stated in `tests/common/reference.rs`'s own module doc as well as here — flips if a cheap way to run `BothOrderings` widely appears, which the dedupe itself makes more likely rather than less.

D-135: Amends D-129's example list, and records the tension a REVIEW-impl surfaced in the taxonomy rather than leaving it to be re-derived. Two corrections. D-129 names "coordinate overflow" among the things `assert!` guards; it is not — the guard is `panic!` inside a `checked_*` unwrap under `COORD_OVERFLOW` (D-34, D-44), and the taxonomy says nothing about `panic!`-in-`unwrap_or_else`, which is in fact the workspace's dominant fail-loud device at some two dozen sites. The taxonomy governs the two ASSERTION macros and nothing else; a named `panic!` on a refused `Result` is a third device with its own rule (rule 3), and reading D-129 as covering it would be reading a classification into a line that never made one. The tension, which is real and is not a defect: `pvs.rs` carries the SAME predicate — the phase is a turn boundary — twice, forty lines apart, as a `debug_assert!` at the depth-0 horizon and an `assert!` at the empty-candidate horizon, with the same consequence on violation (a static value returned mid-turn, which is a wrong answer). Under D-129's primary test the first would be an `assert!`; it is a `debug_assert!` on the cost clause alone, because it sits at every leaf while the other sits in a branch that is unreachable for any radius of at least one (D-104) and therefore costs nothing. That is the ruling as written and it is followed as written; what was missing is the sentence saying the two are the same statement priced differently, which is what D-128's `release-checked` profile exists to collapse — flips if the cost clause is ever measured to be worth less than the confusion, which is a rule-5 bench and a new line here.

D-136: `corpus-extract` is a SECOND BINARY in pistol-cli, not a `pistol` subcommand and not pistol-arena. This amends CLAUDE.md's crate map, which lists pistol-cli's binaries by enumeration — line-protocol engine, perft, bench, selftest — and this is not among them; the amendment is stated here rather than read into the map, because a map that licenses anything licenses nothing. Not a `pistol` subcommand: that binary builds an engine from a config on every path and this one builds none, so the two would share a program and nothing else. The determinism-gate argument for the split is NOT the reason and is recorded as rejected — `tools/determinism.sh` runs named commands and a subcommand that constructs no engine cannot dilute D-7's one-engine-construction path; the real reason is that a one-shot curation tool has no business linked into the program that ships. Not pistol-arena, whose map line does name "paired openings" and is therefore a genuine claim on this work: D-133 cut WP-1.2a to have no runner in it, and the deciding practical fact is that the extractor needs SHA-256 at run time, which pistol-cli already has and pistol-arena does not — putting it there would grow the workspace's third copy of a hash — flips when pistol-arena exists and wants the extractor beside the runner that reads its output, which is a move of one binary and no rules.

D-137: The twelve symmetries of the hex lattice land in pistol-core NOW, as `symmetry.rs`, and the opening corpus deduplicates by `canonical_form` rather than by position identity. The slot was already reserved verbatim in pistol-core's own `lib.rs` for Stage 5's opening book, so this is Stage-5 work pulled forward rather than scope invented, and it is additive: no pinned constant, no perft count and no existing fixture sha moves. It goes in pistol-core because it is lattice geometry and geometry lives there (CLAUDE.md rule 2) — doing it in the extractor would have been the rule-2 breach the convenience tempted. The measurement that forced it, over the RATED GAMES AT OR ABOVE THE FLOOR AND BEFORE THE MISMATCH CEILING (4380 games — the pool named so the numbers can be reproduced without guessing, since D-142's "eligible" is a narrower set): at K=3 that pool holds 2673 distinct positions but only 1934 symmetry classes (27.6% duplicates), at K=4 3681 and 3289 (10.6%), and in a 200-entry capped set 16 of 200 entries were the mirror image of another entry. CLAUDE.md rule 6 counts distinct games with identical games deduped, and an arena deduplicating by move list provably cannot see these, so the reported distinct-n would have been wrong. The counter-argument is recorded because it is true and it bounds the size of the defect rather than removing it: D-7's final tie-break is lexicographic by `(q, r)` and so is NOT symmetry-invariant, which means two mirrored openings often do not produce mirrored games — they are strongly correlated samples, not identical ones. Translation is deliberately excluded from the group (it would collapse K=3's 1934 to 1749): rule 3 anchors every position at the origin, so a translated position is not a position this game reaches — flips if a fixture ever needs shape equivalence rather than position equivalence, which the opening book might.

D-138: An opening is K = 4 TURNS, seven stones, both sides having had two turns. The first design said three turns and gave three reasons, two of which were wrong and are recorded as withdrawn so nobody re-derives them: (a) "K=4 shrinks the distinct pool" is backwards — prefixes are nested, so distinct-position supply is monotone non-decreasing in K, and the measurement — over the same rated-and-floored pool D-137 names, 4380 games — is 535 positions at K=2, 2673 at K=3, 3681 at K=4, with symmetry classes 212 / 1934 / 3289; on the narrower set the ceiling leaves, K=4 is 1739 distinct and 1591 canonical, which is the fixture; (b) "far enough from the origin that the radius-8 region is open" distinguishes nothing, since a radius-8 ball holds 217 cells and the region is wide open after the first stone. What survives is that K=3 hands P2 a position in which P1 has moved twice and P2 once, while K=4 gives each side two turns, and that K=4 has 70% more symmetry-distinct openings at a quarter of K=3's duplicate rate. The honest cost is recorded too: a longer prefix is more committed and therefore more lopsided — of the canonical classes with at least ten corpus games, 33% fall outside a 0.35-0.65 first-player win rate at K=3 and 43% at K=4. K is the wrong instrument for balance, which is what D-145 is for — flips if WP-1.2b measures transposition between shallow openings, which would argue for going deeper still.

D-139: The corpus reader accepts the SCHEMA'S grammar, not JSON's, and every deviation is a named refusal. `serde_json` was never an option — `crates/pistol-cli/tests/workspace_shape_tests.rs` pins every crate in this workspace to `pistol-*`, `serde`, `toml`, `serde_path_to_error` — but the choice that mattered was between a general hand-rolled JSON value parser and a narrow one, and the first design chose the general one on the grounds that ad-hoc parsing turns malformed input into wrong answers. The conclusion was right and the inference inverted: a general parser's risk lives in the features this corpus never uses — `\uXXXX` and surrogate pairs, exponents and fractions, nested containers — which is surface no test here would exercise and no corpus would reveal, whereas a reader accepting only the schema's shape CANNOT silently mis-read, because anything else is a refusal. So no escapes, no floats, no exponents, no nesting beyond the schema's, a `\r` refused rather than trimmed, a blank line refused, and `source` pinned to `human`; `SCHEMA.md` permits `elo` to be absent under `--no-meta` and this tool requires it, which is a stated narrowing rather than an assumption. It also keeps the file under rule 9's cap, which the general parser would not have — flips if a future corpus legitimately needs a JSON feature, at which point the reader grows that feature by name.

D-140: A corpus move's element zero is `q` and element one is `r`, pinned by a parser test on a move whose components differ. This is a rule-3 line rather than a triviality, because NOTHING DOWNSTREAM CAN CATCH THE SWAP: q<->r is one of D-137's twelve lattice automorphisms, verified to preserve hex distance and to permute the three axes onto themselves, so a reader with the mapping backwards produces a perfectly legal, perfectly self-consistent, REFLECTED corpus — every game replays, every winner agrees, zero exclusions, and the replay oracle reports success. The oracle's blindness is named in the module doc beside the test, because the oracle is otherwise this work package's strongest instrument and a reader could reasonably assume it covers this — flips never; it is a fact about the geometry.

D-141: The recorded-order cross-check counts two things `make_turn` cannot see, and each has a fixture that makes its count exactly one. A pair is legal iff SOME ordering of its placements is (D-6, D-51), so the turn-level oracle is the right eligibility filter and is structurally blind to what the record's own order did: **order-rescued**, where the recorded first stone is outside the legal region and the pair plays reversed, and **stone-after-win**, where the recorded first stone already completes a line so the record's second stone follows a decided game while the same pair played the other way round is an ordinary turn. Both measure zero on the real corpus, and that is only worth stating because the two fixtures show the counters moving: a REVIEW-design finding, verified with its own reproducer, was that an implementation hard-coding both counts to zero would pass every other test, pass the real run and print the headline — an unfalsifiable measurement recorded in an ADR being worse than no measurement. The check folds into the single replay pass as a `place` / classify / `undo` probe rather than a second full replay, which also removes the question of what a second replay does after it diverges. Exhaustiveness is proved rather than assumed, and this line states the mechanism the code actually uses rather than the one an earlier draft described: the two classes are read from what the probe DID — a refusal with `OutsideLegalRegion` is order-rescued, and a SUCCESSFUL `PlyOutcome::Win` on a turn that has a second stone is stone-after-win. `GameDecided` is not the stone-after-win signal and is in fact unreachable, because the loop returns `PostWinContinuation` at the top of every iteration before any probe; it panics, as do `OccupiedCell` and `FirstStoneNotAtOrigin`. The statement order is load-bearing — the probe's verdict is matched only after `make_turn` has returned `Ok` — and `replay.rs`'s module doc now says so, because hoisting that match for readability would turn an ordinary named exclusion into a panic — flips never.

D-142: Opening eligibility is a TWO-SIDED rating band: `|elo_p1 - elo_p2| <= 100`, then `min(elo) >= M` where M is the lower median of `min(elo)` over the games meeting every other clause (element `(n-1)/2` of the ascending sort — an integer, no averaging, no rounding rule). The floor alone, which the work package specified, was measured INERT and that finding is the reason the ceiling exists: with the priority rule and a 200-entry cap the emitted set was byte-identical with and without it, because the 200th pick had min-elo 1310 against a median of 996. The ceiling is derived from the corpus and the criterion was fixed before the yield was read: the higher-rated player scores .532, .558 and .609 in the gap buckets below 100 and then .675, .747, .860 and .941 above it, so 100 is the largest round gap at which every bucket keeps the favourite under .61 — below it an opening is a contest both sides contributed to, above it one side was outclassed and half the opening is that player's mistake. The comparison is INCLUSIVE (`gap <= 100`) while the buckets that derived it are half-open, so the 30 candidate games at a gap of exactly 100 sit in the .675 bucket and are admitted; the discrepancy is named rather than left for a reader to find, and it is 30 games in 3908. Consequences, not reasons: 3908 candidates, M = 997, 1956 eligible games. What the band does NOT buy is balance, and the ADR says so — the first-player win rate is 0.5022 overall, 0.5021 above the median and 0.5023 below it — flips if a corpus arrives whose ratings are on another scale, since 100 is an Elo distance and not a quantile.

D-143: The openings fixture has NO CAP and is emitted in `game_hash` order. The work package specified a cap of 200 by a priority of higher min-elo then lower hash; both REVIEW-design agents found the number underived, and the measurement behind dropping it is that in a deterministic instrument mode the book is the only source of game diversity, so 200 openings is 400 games and no more — about 40 normalized Elo of resolution, where the research report's SPRT bounds want thousands. A cap that cannot be relaxed downstream is a ceiling on Stage 1's whole ability to accept or reject its own work, and there is no size argument for it: the full pool is 1591 openings and about 140 kB, some 1.4% of rule 7's ceiling. The priority rule survives with its real job — choosing which game REPRESENTS a canonical class — and the emission order is the hash instead, because with no cap a runner takes a prefix, and a prefix of a rating-sorted pool is the extreme tail rather than a sample (the 200 openings the old rule would have emitted came from a 239-game slice of 8698). WP-1.2b pre-registers its own n in its own validated config, where CLAUDE.md rule 1 already governs it — flips if a fixture consumer turns out to need a pre-committed subset, which is a second file and not a truncation of this one.

D-144: SHA-256 moves from pistol-cli's test tree into the crate's library as a PUBLIC module. WP-1.2a turned the digest from something a test checks into something a program computes — `corpus-extract` hashes the corpus it read and the payload it wrote — and a digest a binary needs at run time is library code. It is `pub` rather than `pub(crate)` against a REVIEW-design recommendation, and the reason is recorded because the recommendation's premise is right in general: this crate's public surface is the contract the API layer will adapt (rule 11) and a hash is no part of it. But an integration test is a separate crate and cannot see `pub(crate)`, and `tests/tactical_v0_tests.rs` pins the tactical fixture with this digest, so hiding it would have left that test needing a second copy INSIDE pistol-cli — precisely what promoting the file was meant to stop. The FIPS 180-4 vectors move with the implementation as a `#[cfg(test)]` unit test, so the hash that computes every pin is still itself pinned. D-37 is untouched and does not apply: it makes pistol-core std-only including dev-dependencies, so that crate's test tree keeps its own copy and cannot do otherwise — flips if the workspace ever takes a hashing dependency.

D-145: The openings fixture EMITS the corpus's own outcome evidence per opening — how many corpus games reach it up to a symmetry, and how many of those the first player won — and acts on none of it. ROADMAP's WP-1.2a line and D-133 both name "balance" as this package's charter, and the first design's answer was that paired play makes it moot. That is half right and the wrong half is expensive: playing each opening from both sides cancels colour BIAS exactly, but it does not recover the power lost to a lopsided opening, because under determinism a decided opening yields a fixed 1-1 pair carrying no information — and D-143 has already established that the games cannot be bought back by playing more. The corpus says human openings are not balanced: of the canonical K=4 classes with at least ten games, the first-player rate runs 0.10 to 0.91 and 26 of 61 fall outside 0.35-0.65. Emitting the evidence rather than thresholding on it is deliberate — a threshold is the runner's to pre-register, and the report's literal recipe (filter by |eval| below a balance threshold) would put the engine under test into its own book. The coverage limit is stated in the fixture header rather than implied: of 5941 canonical classes, 885 have two or more games and 61 have ten or more, so most lines read `games 1` and the evidence speaks for part of the pool — flips when an engine strong enough to be trusted as a neutral judge exists, which is a Stage-5 book question.

D-146: A bench position is the prefix at the LARGEST ODD STONE COUNT at or below the band centre and no lower than the centre less the width, strictly fewer than the game's own stones. This is the rule the work package's "15±2 / 35±5, first index entering the band" actually behaves as, spelled the way it behaves: a position must sit at a turn boundary to be a move list at all (D-6) and turn t carries 2t-1 stones, so only odd counts are reachable, and "closest to the centre" always lands at or below it — on the real corpus band 15 selects 15 in all 8698 games and band 35 selects 35, 33 or 31 and never 37 or 39. Shipping a symmetric ± of which half provably never fires would have been shipping a parameter that does nothing. Twelve per band, in `game_hash` order because a content hash cannot be cherry-picked, and NO GAME IN BOTH BANDS: twenty-four games measure twenty-four position shapes where twelve measured twice would measure twelve, at the same cost. A REVIEW-design proposal to stratify the selection by legal-region size was declined — it is a curation choice nobody pre-registered — and the concern it raised is answered instead by reporting each selected position's stone count in the file. Named consumer, which the first design lacked: ROADMAP WP-1.3(c)'s fixed-node runs at two stone counts, D-127 having already measured against two ad-hoc positions — flips if WP-1.3 wants a third band.

D-147: Both fixtures are serialized by a stated discipline, because a digest pinned over an under-specified serialization pins an accident (CLAUDE.md rule 4). Lines end `\n` and the file ends with exactly one; the header identifies the corpus by SHA-256 AND BY NOTHING ELSE — no path, no timestamp, no hostname, no version, no git SHA, so two machines given the same bytes write the same file and a header carrying a path would have made "identical inputs, identical outputs" false between two machines that both did everything right; `# param` lines carry inputs and `# derived` lines carry computed values, so a reader reproducing the file can tell a choice from a measurement; the exclusion list is ordered by game hash, capped, and written `# excluded none` when empty, because a missing section and an empty one must not look alike; and the body begins at the first byte after the newline ending the `# body_sha256` line, which is stated because the payload contains `#` comment lines of its own and "the leading run of comments" would not have been a usable rule — flips never without a new fixture version.

D-148: Each fixture carries a `# body_sha256` over its payload AND is pinned by whole-file digest in the test that reads it. The first design justified the pair by saying they catch different edits, which is wrong and is corrected here: the whole-file digest changes on any byte anywhere and so strictly dominates for edit detection. The body digest earns its place for a different reason — it is IN-BAND, so a consumer holding only the file, which is what pistol-arena will be at load time, can refuse a corrupted fixture without carrying an out-of-band constant from someone else's test tree (rule 3). WP-1.2b is expected to verify it; if it does not, the body digest is dead weight and should go — flips on that.

D-149: The replay result, which is this work package's most valuable output and the reason the extractor exists at all. All 8698 games of the human corpus replay through pistol-core under the full rule set — turn structure, occupancy, origin opening, win detection, rule 4's truncation, and the radius-8 legal region — with ZERO failures in every class: zero illegal turns, zero post-win continuations, zero winner mismatches, zero games left undecided, zero order-rescued turns and zero stones recorded after a winning one. The winner pistol-core computes agrees with the corpus's independently exported `winner` field on all 8698. This is the first independent evidence for D-101, which resolved the radius-8 question by operator ruling against an evidential gap: radius 8 appears nowhere in the corpus's `SCHEMA.md`, `README.md` or `dataset_metadata.json`, and 8698 real platform games now replay inside it without one exception. Rule 4 fires in the wild: 151 games end on a turn's first stone with the second never played. Alongside, the first look at the report's section-F fairness question, WITH ITS CONFOUND NAMED rather than claimed as an answer — first player wins 4368/8698 (0.5022) overall, 2199/4380 above the rating median and 2169/4318 below it, and among evenly matched games (rating gap under 25, n = 1096) 530/1096 (0.4836, ±2.96% at 95%), mildly defender-leaning and not distinguishable from even. A rated human corpus CANNOT answer the game-theoretic question: seat assignment is independent of strength, so the aggregate is about even by construction of the rating pool whatever the game's intrinsic tilt, and the confound is visible in the same data — among the games D-142's ceiling excludes, gap strictly over 100, the higher-rated player scores 3856/4790 (0.8050). (At gap 100 or more, which is the half-open bucket D-142 derives from, it is 3877/4820 (0.8044); the stats block reports the first, because that is the population the ceiling removes.) Section F stays open for engine self-play in WP-1.2b — flips on a corpus that does not replay clean, which is a rules-truth escalation and not a data problem.

D-150: Puzzle mining is DEFERRED to a named second pass, pre-registered here so it is not re-proposed as an oversight. Mining mate-in-n and must-block positions over every position of every corpus game needs fixed-depth sweeps that are unaffordable full-width — WP-1.1's own oracle pays the candidate count squared per turn, which is minutes of release CPU for a third turn on ONE position — so the pass belongs after WP-1.5's threat machinery makes a bounded forcing search cheap, and it reads the same corpus by the same sha. What WP-1.2a ships instead is the two fixtures the arena needs now; the tactical fixture class those puzzles would join is `tactical_v0.txt`'s successor and will pre-register its own threshold below 100%, per that file's own header — flips when WP-1.5 lands and the sweep is affordable.

D-151: `openings_v1.txt` is a FIXTURE under CLAUDE.md rule 7, not a book under rule 8, and the distinction is written down here because it is exactly the kind rule 10 exists for. Rule 8 says nets, books, match logs and bench outputs are never committed; this file is a committed list of opening positions, so the question is fair. What makes it a fixture is what is in it: corpus provenance and nothing else — a position, the game it came from, that game's lower rating, and the corpus's own win counts for its symmetry class. No engine judgement, no eval, no search result, no solved value. A book is a store of the engine's OWN conclusions and is regenerated whenever the engine changes; this is a store of what humans played, is regenerated only when the corpus changes, and is pinned by digest precisely so that it cannot drift (D-147, D-148). It is 140 kB against rule 7's 10 MB ceiling. The corpus it derives from stays uncommitted, which is the rule-8 obligation that does bind here — flips the moment a curation step consults an engine, which D-145 declines to do for its own reasons; a balance-filtered book WOULD be a book.

D-152: Records the WP-1.2a review round, because three of its findings are amendments to lines above rather than defects in code, and a reader of D-136..D-151 should know which sentences were corrected and why. REVIEW-design attacked the four parameters the work package invited attack on and all four moved: K went 3 -> 4, the cap went 200 -> none, the dedupe key went position identity -> canonical form, and the elo rule gained a mismatch ceiling — each recorded in its own line with the measurement that moved it. REVIEW-impl then found that the committed fixtures had been generated BEFORE the ADR citations in their own header prose were renumbered, so the shipped artifacts cited D-144/D-146/D-147 where the source now said D-143/D-145/D-146, and the sha pins certified bytes the build could no longer produce — a rule-4 and rule-7 breach that the whole suite was structurally unable to see, because every header test read the committed bytes and never bytes the tool had just written. That hole is now closed by two tests over produced output, one of which compares the header prose block against the committed one, so an edit to the prose without a regeneration is a red test. The same pass found four curation clauses whose tests asserted a constant against itself — `gap <= ELO_GAP_CEILING` is true whatever the constant holds — and each is now pinned by its consequence instead; the surviving mutations are listed in the session report. RED-TEAM ran some seventy adversarial documents and found every malformed one refused by name at exit 2 and every rules-level failure excluded by name at exit 1, but caught an empty candidate set aborting through an `assert!` at exit 101 and discarding the stats block, which is the escalation output — now a named refusal that prints the measurement first — flips never; this line is history, and the lines it describes are the record.

D-153: The arena's budget section spells all THREE budget kinds, including the `movetime_ms` it refuses, and the refusal is its own named error citing the reason. Leaving the variant out of the schema would have been shorter and would have made `serde` answer "unknown variant, expected one of depth_turns, nodes", which points at the schema and tells an operator nothing about why wall-clock is not admissible here. This is D-22's shape moved up a layer: the enum is closed and complete, and the layer that cannot honour a member refuses it by name rather than by omission. The text cites CLAUDE.md rule 6 (every strength claim comes from an instrument budget) AND docs/decisions.md D-95 (a movetime budget is a floor rather than a ceiling, because the first deepening iteration is not interruptible), because those are two different objections and an operator who worked around the first would still be caught by the second. `arena_refuses_movetime_budget_with_named_error` additionally asserts that the same validator ACCEPTS `depth_turns` and `nodes`, so it cannot pass against a validator that refuses every budget — flips by the ADR that lets a wall-clock run make a strength claim, which is WP-1.4 and Stage 4 at the earliest.

D-154: The verdict is read off the PAIR-level GSPRT and the game-level LLR is demoted to a reported diagnostic. OPERATOR RULING, and it overrides the work package's own pre-registration, which said the v0 verdict would be game-level; the ruling is recorded here rather than in the code because changing a pre-registration is an operator act. Both REVIEW-design agents attacked the pre-registered choice independently and the design's defence of it did not survive. What the design asserted — that the game-level test is "roughly a factor of two anti-conservative" — is wrong, and wrong in DIRECTION as often as in magnitude. The derivation, since an asserted magnitude is what got this wrong the first time: write the LLR as an evidence term minus a drift term; the sample mean is identical at both units, so the two differ only through sigma, and `var_pair <= var_game` always (with equality only when no pair is 1-1). The factor of two is therefore a strict upper bound attained only at perfect within-pair correlation, and once 1-1 pairs are common the game-level test is CONSERVATIVE. Measured on a constructed sample of 200 pairs of which 150 are 1-1: `llr_game` 1.1625 against `llr_pair` 1.8535, the game-level statistic understating the evidence by 37%. Two deterministic engines at a fixed budget produce exactly that regime, so the direction an operator would have guessed is the wrong one. The rejected alternatives are recorded because this ruling is re-openable: (a) KEEP THE GAME-LEVEL VERDICT as pre-registered and report the disagreement factor `2*sqrt(var_pair/var_game)` beside it, which is honest and costs nothing but ships a verdict known to be wrong in a direction the run itself determines; (b) REQUIRE BOTH UNITS TO AGREE, verdict `inconclusive_split` otherwise, which is strictly more conservative and spends real games on the finite opening pool to buy certainty the SPRT already prices. Reporting both without a binding rule was rejected outright on a reviewer's argument that `nElo_pair >= nElo_game` unconditionally, so an operator can predict which number flatters a result before running it, which is a garden of forking paths inside the tool whose whole job is to stop one — flips at Stage 5's pentanomial manager, which changes the scheduling around this statistic and not the statistic.

D-155: The estimator is the closed-form Gaussian GSPRT in normalized Elo, `LLR = n * ((t1 - t0) * (mu - 1/2)/sqrt(var) - (t1^2 - t0^2)/2)` with `t_k = elo_k * ln(10)/800`, and not the exact multinomial GSPRT. The deciding reason is this work package's own requirement that the math be pinned by a table of HAND-COMPUTED values: the exact GSPRT needs a constrained maximum-likelihood tilt found by a one-dimensional root search, which nobody computes by hand, and a reference table that can only be produced by the implementation under test pins nothing. The scale factor is `800/ln(10)` and not `1600/ln(10)`: from `mu = 1/(1+10^(-Elo/400))`, `mu - 1/2 ~= Elo*ln(10)/1600`, and normalized Elo is defined to equal logistic Elo at `sigma = 1/2`, where `t = (mu-1/2)/sigma = 2(mu-1/2) = Elo*ln(10)/800`. Both reviewers re-derived it independently and it is also consistent with the research report's `640000/nElo^2` rule of thumb, which `1600/ln(10)` misses by a factor of four. The pair unit uses `t_k * sqrt(2)`, which is not a fudge: normalized Elo is defined on the per-GAME scale, so a pair's hypotheses must be written in per-game sigma, `sigma_per_game = sqrt(2 * var_pair)`, and substituting reproduces the same expression with the hypotheses scaled. `the_pair_unit_answers_the_same_hypothesis_as_the_game_unit` pins that the two agree exactly on an independent-limit sample, which is the property the scaling exists to give and which a missing `sqrt(2)` would break. The estimator is a PROFILE likelihood ratio, sigma being re-estimated at every prefix, so the Wald bounds are approximate exactly as they are in Fishtest, and the module says so beside them. Arithmetic is bit-reproducible by correct rounding and a fixed expression order rather than by exactness: no `mul_add`, no reordered summation, variance in the raw-moment form the reference table pins, `ln(10)` a compile-time constant, and the only libm calls in a whole verdict are the two Wald bounds — so a verdict could differ across libm implementations only when an LLR lands on a bound to the last bit — flips at Stage 5, where an exact GSPRT can be cross-checked numerically against an external implementation instead of by hand.

D-156: There is NO regularization of zero counts. A sample with zero variance has no likelihood ratio, and the verdict token says `inconclusive_degenerate` rather than carrying a number. OPERATOR RULING. The design proposed Fishtest's pseudo-count of 1e-3 and argued it was part of the estimator's definition rather than a tunable; a reviewer refuted that with a reproducer on this work package's own smoke shape. At `n = 8`, `w = 8`, `elo1 = 4`, the LLR is 1.163 at a pseudo-count of 1e-2 and does not cross, and 3.683 at 1e-3 and crosses to `h1`, scaling as the pseudo-count to the power of minus a half. A constant that flips a verdict is a tunable whatever an ADR calls it, and a tunable lives in exactly one schema place (CLAUDE.md rule 1). The Gaussian likelihood ratio is meaningless in that regime anyway: the sample mean sits some forty sample-sigmas from BOTH hypotheses and the statistic still reports evidence. The rejected alternatives, since this is re-openable: (a) MAKE IT AN EXPLICIT `[sprt]` KEY with no code-side default, which satisfies rule 1 and hands an operator a knob whose statistical meaning is nil; (b) REFUSE PLUS AN EXPLICIT `min_pairs_before_stop`, also pre-registered, which additionally keeps the sequential rule out of the small-sample regime. (b)'s second half turned out to be unnecessary rather than wrong: a degenerate sample cannot cross a boundary either, so the refusal is already the stop guard and the spurious early crossing a reviewer demonstrated cannot occur. The stated cost of the ruling is accepted: an engine that wins every game reports `inconclusive_degenerate` forever, and the report's `8-0-0` and its pentanomial are what an operator reads instead — flips if a run ever legitimately needs to conclude from a shutout, which would be an argument for (a) with the constant pinned by its own reference-table row.

D-157: `run.openings_take` is the run's only size knob and the game cap is derived as twice it; `capped_fraction` is REPORTED and not thresholded on. Two decisions in one line because both are about not stating a number twice. The first: every opening is exactly one pair, so a separate `game_cap` was inert config wherever it was not simply `2 * openings_take`, and its evenness rule then meant two different things in two places — a reviewer found the design's own stopping rule violating the parity rule its own config validation enforced. The second is an OPERATOR RULING, and it is the one place this work package leaves a known lever unguarded. `turn_cap` is pinned as an evaluation horizon and never a game rule, but capped games are not inert in the statistic: normalized Elo is variance-normalized, a mass point at a half shrinks sigma, and for a FIXED decisive record adding capped games ACCELERATES the LLR toward H1 — measured, a 100-80 record scores 0.2198 with no capped games, 0.6329 with 1800, and about 1.111 at the maximum. The design's first draft stated this cost BACKWARDS, claiming capped games shrink the effective sample, and that sentence is withdrawn here so nobody cites it. The rejected alternative is a pre-registered `capped_fraction_ceiling` in `[sprt]` yielding a verdict of `inconclusive_horizon` above it, which is what a reviewer proposed and what rule 5's no-post-hoc-thresholds argument favours; the ruling is to report the fraction and let an operator judge the run, which trades that guard for less schema. What makes the unguarded version survivable is that an all-capped run is degenerate under D-156 and so cannot report `h0` or `h1` at all — flips the first time a real experiment's verdict turns on its capped fraction, which the reported number will make visible.

D-158: A forfeit is a game outcome AND it invalidates the run's verdict token, not merely its exit code. The offender loses the game, and the report carries the offending party, the reason, the turn and the engine's verbatim refusal — the work package pre-registered "a named reason in the report", and aborting the run instead would write no report at all. But a forfeit in a pistol-versus-pistol match means one of the two configurations is broken, and a reviewer's reproducer is decisive: point engine B at a stub that always plays an illegal turn, and every game forfeits, A scores 8-0, the LLR crosses, and `grep '^verdict ' report.txt` answers `h1`. A count on a neighbouring line does not fix that, and neither does an exit code, which is gone by the time a report is pasted into an ADR. So the token itself reads `invalid_forfeit`, the would-be conclusion moves to a separate `verdict_if_clean` line — hiding it would be the silent skip rule 3 forbids — and the process additionally exits non-zero because a script reads that and a human reads the file. `result` and `end` stay two fields for the same reason: a forfeit is a LOSS, so the winner is known, and collapsing them into `result forfeit` would throw the winner away and leave a reason with no party attached to it — flips if an external opponent is ever admitted (Stage 5+), where a forfeit is an ordinary match event rather than evidence of a bug in one's own engine.

D-159: Silence abandons the RUN and is never adjudicated; a child that EXITED WITH A CODE forfeits, and a child killed by a SIGNAL or a spawn that failed abandons the run like silence. The first half is the determinism law applied to the referee: a timeout that forfeited a game would make the verdict a function of how fast the machine is, so the same two engines over the same openings would report differently on a loaded box and an idle one — inside the instrument every strength claim comes from (CLAUDE.md rule 4). The second half is a reviewer's finding against the design's first draft, which forfeited on any closed pipe. The counter-example is concrete: run at eight workers on a memory-tight box and the OOM killer takes children, which arrives at the arena as an EOF indistinguishable from `exit(1)` — so the design's stated distinction, that a closed pipe is "a deterministic answer to a deterministic input", was an assumption about the ENVIRONMENT rather than a property of the protocol. `ExitStatus::code()` tells them apart on Unix, returning `Some` for a self-chosen exit and `None` for a signal, and the exit code or the signal is recorded either way because `engine_exited` with no status is undiagnosable. `hang_timeout_ms` is explicit config with no default; it satisfies rule 1 because it has a schema place, and it satisfies rule 4 because it can never reach a reported number. There is no wall-clock-free liveness signal available to replace it: D-2 pins the verb set with no stop and no heartbeat, and a truly hung engine emits nothing at all — flips never while instrument mode is the source of strength claims.

D-160: A run that is abandoned still writes a report, whose FIRST-LINE TOKEN is `arena_report_aborted` rather than `arena_report`, and which carries no `verdict` line and no LLR. The design's first draft wrote nothing, which both reviewers attacked from the same direction: a hang on game 1500 of 1591 would discard every completed game, and D-159's actual requirement is only that no REPORTED NUMBER depend on wall-clock — not that the evidence be destroyed. A different first-line token rather than a flag on the same one, because a consumer that greps for `verdict` must not be able to find one on a run that did not produce one, and because the games in an aborted report are a diagnostic rather than a sample: WHICH games finished before the abort depends on timing, and the file says so in a comment line above them. `arena_aborts_run_when_engine_hangs` asserts the kind token, the absence of any `verdict` line, and the presence of a named `aborted Hung` line — which is a stronger assertion than "no file exists", and one that cannot pass vacuously — flips never.

D-161: The report splits at a `# timing` marker into a verdict block that is worker-invariant and byte-comparable and a timing block that is machine- and schedule-dependent, and the invariance is claimed only for runs that COMPLETE. The claim is stated with that qualification because it is false without it: a watchdog sized against one worker can fire at eight, and then two worker counts produce a report and nothing rather than two equal reports. Engine `time_ms` cannot be invariant — two games contending for a core change it — and neither can the count of in-flight games abandoned at the stop, so they are named, segregated and excluded by construction rather than wished away. This is what `tools/determinism.sh` already does with `nps` and `time` (D-7). One consequence was found by the test rather than by the design: the arena config's own digest is NOT invariant either, because `n_workers` lives in that document, so the verdict block carries an `experiment_sha256` over the experiment-defining values only — the openings body digest, the take, the turn cap, the budget, the SPRT bounds and each engine's binary and config digests — while the document's own digest is provenance and sits in the timing block. Identifying engines by DIGEST rather than by path is the same rule D-147 applied to the corpus: `target/release/pistol` is a different program after every build, and a report that named it by path would identify two experiments identically — flips never.

D-162: The arena refuses, at the handshake and before a single game, an engine that reports a protocol version it does not speak or a mode other than `instrument`. The mode check is the load-bearing one: every strength claim comes from an instrument mode (CLAUDE.md rule 6) and instrument mode is where `threads = 1` is enforced (D-7), but a play-mode engine given a depth budget is a perfectly legal request that nothing else in the stack questions — the engine's own `Budget::check_supported` refuses only a wall-clock budget — so this is the only place the rule can be enforced at all. The version check exists because the two sides may be DISTINCT BUILDS, which is the point of naming a binary per configuration, so the arena may not assume the program it spawned speaks the protocol it was compiled against. Both are covered by a stub behaviour rather than by inspection, because a version comparison against an in-tree binary always passes and deleting it would turn nothing red — flips when a deterministic multi-threaded scheduling mode exists (Stage 4), which would be a third mode token and its own line here.

D-163: The distinct-game key is the canonical form of the ORDERED TURN SEQUENCE under the twelve lattice symmetries, and it lands in pistol-core as `canonical_sequence` beside `canonical_form` rather than being composed in the arena. It is not `canonical_form`, and the design's first draft said it was: that function takes a stone MULTISET, sorts the image, and documents that "the order they were played in is not part of the identity this module is about" — so keying a game on it would dedupe by final POSITION and merge two different games that reached the same stones, understating distinct-n, which is the opposite of the error symmetry folding exists to prevent. A reviewer found this by reading the function rather than by running anything, and `two_games_reaching_the_same_stones_are_two_distinct_games` pins it with two games that differ only in the order the first player played its two pairs — the fixture's first draft got even that wrong by redistributing stones ACROSS turns, which changes their colour and makes the positions differ too, so the test would have proved nothing. It lives in pistol-core because it is lattice geometry and geometry lives there (rule 2); doing it in the arena would have been the convenient rule-2 breach. A transformed pair is re-canonicalized to D-5's `(q, r)` order, because a symmetry does not preserve that order and a key that skipped the step would not be symmetry-invariant at all — `a_transformed_pair_is_returned_in_canonical_order` additionally asserts that some symmetry in its fixture actually reorders a pair, so it cannot pass on a fixture the group happens to leave alone. The limitation is stated rather than implied: the key has no false positives and it does have false NEGATIVES, because D-7's final tie-break is lexicographic and therefore not symmetry-invariant, so two mirrored openings usually do not produce mirrored games (D-137 recorded the same thing). `distinct_n` is therefore an over-count and a bound rather than a census — flips never.

D-164: Every game gets FRESH subprocesses; a channel is never reused across games. Reuse plus `newgame` would be faster and D-7's gate already certifies that `newgame` clears everything, but under N workers the assignment of games to processes changes with N, so any residue `newgame` failed to clear would make the report depend on the worker count — the one thing the scheduler must guarantee it does not (D-161). A process spawn is microseconds against a search. There is a second reason the design did not name and a reviewer did: the claim that a self-match's two games are identical move for move rests on both first-seat processes receiving byte-identical command sequences from a fresh start, and the in-game transposition table persists across turns — flips if process spawn is ever measured as a real fraction of a run (rule 5), at which point the fix is a worker-invariant assignment and not naked reuse.

D-165: The sequential stop is evaluated ONLY at pair boundaries, and `k` is the SMALLEST pair count whose sample crosses, recomputed over the whole finished prefix rather than taken as the first crossing a worker happened to notice. Both halves are needed and both were reviewer findings. Odd `k` splits a pair — one seat's game in and the other's out — and the truncation point is correlated with the outcome of the last included game, since a crossing is likeliest immediately after a decisive one, which is optional-stopping bias applied to the one game whose seat is unmatched; it also destroys the pentanomial the verdict now depends on (D-154). Smallest-crossing rather than first-noticed is what makes `k` a function of the game results alone and therefore independent of the order games finished in, which is what the worker-invariance claim rests on. Games in flight past `k` are killed and discarded; they never contributed to the report, so discarding them cannot bias it, and their count is schedule-dependent and lives in the timing block — flips never.

D-166: `arena-stub-engine` is a second binary in pistol-arena, and this amends CLAUDE.md's crate map the way D-136 amended it for `corpus-extract`. The map's pistol-arena line names paired openings, SPRT, Elo, distinct-game dedupe and per-side compute accounting, and a deliberately misbehaving ENGINE is none of those, so the amendment is stated here rather than read into the map. It exists because the forfeit, garbage, bad-bestmove, exit, hang, wrong-protocol and play-mode paths are otherwise unreachable by any test, and a referee nobody has watched refuse is not a referee. Its protocol conformance is the engine's own code — it implements the `Engine` trait with a one-line search and serves it through the real `pistol_cli::Session`, so only the named deviation is its own. A bin rather than an example because `CARGO_BIN_EXE_<name>` exists for bins only, and a test that guessed at `target/<profile>/examples/...` would break on a profile change. Two guards against it being mistaken for an engine: it refuses to start without an explicit `behave` line in its config, so it can never default to looking like a very weak engine, and it announces itself on stderr. Its behaviour comes from that config file rather than from an environment variable so that two tests running at once cannot change each other's engine. Its search is deliberately NOT `generate_turns`, which enumerates the radius-8 region pairwise and is minutes per move at seven stones; it grows the mover's own cluster instead, which is affordable AND makes games end in wins, where taking the lexicographically smallest legal cells would have produced nothing but capped games and left the win path untested — flips if cargo ever hands tests a resolved path to an example, which would make D-25's precedent apply.

D-167: pistol-arena depends on pistol-cli, and `pistol_arena_manifest_names_only_core_engine_and_cli` pins that it depends on nothing else in this workspace. The dependency is on the crate that owns the LINE PROTOCOL's one spelling (D-5) — the verbs, the `info`/`bestmove`/`error` prefixes, the `totals` marker, the budget words, and the SHA-256 D-144 already made public for exactly this kind of run-time use. A second spelling of those tokens inside the arena would be a second protocol, and the two would drift silently. It is not a rule-11 breach: pistol-cli is not a transport, it is the contract, and the arena is a client of it. The pin is a separate finding: `workspace_shape_tests.rs` already admitted any `pistol-*` dependency by prefix, so without a test of its own the arena could grow a pistol-search or pistol-eval dependency and stay green — the same reach past the seam that `pistol_cli_manifest_names_only_core_and_engine` exists to stop for the CLI — flips if pistol-api needs the same tokens, at which point the protocol vocabulary moves to pistol-engine and both crates adapt.

D-168: The config gate learns a third document kind. `configs/` now holds engine configs, eval weight tables and arena configs, told apart by file name — `*_weights.toml`, `arena_*.toml`, everything else — each going to its own validator, because a document checked against another schema is rejected for every key it does not have (D-64's argument, now with three schemas). The arena's validator is an EXAMPLE rather than a subcommand of `arena`, on D-25's precedent: config checking is a tools-side gate and the binary's surface should not grow a general maintenance command to hold it. In default mode a missing arena config is a gate failure like a missing weight table, because `tools/arena_smoke.sh` runs a committed one — flips never.

D-169: `tools/arena_smoke.sh` is CI gate 11, and it is a SELF-MATCH because that is what makes it assertable. Two identical deterministic engines play the same game whichever seat they sit in, so the whole expected answer is knowable before the run: n is twice the openings, distinct-n is exactly half of n, every pair scores 1-1, the pentanomial's middle bucket holds every pair, and the verdict is `inconclusive_degenerate` — which is the CORRECT answer for two identical configurations and not a failure. A gate whose expected output is "something plausible" is not a gate, so each of those is asserted exactly rather than as a "near-total dedupe" nobody can falsify. It reads the numbers it asserts against out of the config rather than restating them, so it cannot pass after somebody edits the document. It also runs the arena three times: twice at the config's worker count to cover the arena's OWN determinism, which nothing else in this suite covers and which matters because the arena is the instrument every strength claim comes from, and once at one worker to cover D-161's worker-invariance claim — with a positive check that each run recorded its own worker count, so the comparison cannot pass against a report that simply omits the field. Pre-registered cost (rule 5): three runs at `depth_turns 1` and candidate radius 1 over four openings, 24 games and about 480 searches; measured at 3.2 s wall on the development machine including the release build being already warm. The cost grows with `turn_cap` because the candidate set grows with the stone count, so a change to that key is a change to this gate's runtime — flips if the gate's runtime ever crowds out a correctness gate, at which point the openings count is the knob.

D-170: Amends D-165's letter in one clause and nothing else. That line says games in flight past the stop point `k` are "killed and discarded"; they are not killed. The stop is checked before a worker DISPATCHES a game, so a game already running when the bound is crossed is played to completion and then dropped by the truncation. Correctness is untouched — the record never reaches the report, which is what D-165's bias argument actually needs — but the wall-clock cost is real at a worker count above one, and a reader who took the word "killed" at face value would mis-estimate what a stopped run costs. The clause should read: games in flight past `k` are PLAYED OUT and discarded. The alternative, actually killing them, needs a per-game cancellation signal the `Engine` seam deliberately does not have (D-2 pins the verb set with no stop), so it is a Stage-4 question and not a wording fix — flips when a stop verb exists, which Lazy SMP brings.

D-171: Records the WP-1.2b review round, because two of its findings were defects that no gate could see and two more were ADR sentences the code did not honour. REVIEW-impl, against a named revision, in a separate worktree.

**Blocker 1, and the worst defect in the work package.** A closed pipe and an exited process are two events. The reader thread ends at EOF on the child's stdout, which a child can produce while continuing to run; the reaper then called `Child::wait` with no bound, in a place the watchdog cannot reach because control has already left the receive. So an engine that closed its output and kept running hung the arena forever with NO report written — defeating the liveness device D-159 promises and the evidence-keeping D-160 promises, and hanging CI gate 11 rather than failing it. Reproduced with a nine-line shell engine that handshakes and then closes stdout. Fixed: the reap is bounded by the run's own `hang_timeout_ms`, after which the child is killed and classified as a signal death, which routes it to abandon-the-run — a child that stops answering and will not die is machine-dependent in exactly the way a timeout is, and must not adjudicate a game. Pinned by `arena_abandons_a_run_when_an_engine_closes_its_pipe_and_keeps_running`, which asserts a wall-clock bound as well as the report's kind, because an unbounded wait fails no other assertion.

**Blocker 2.** `verdict_if_clean` filtered individual games out of a flat slice. The pentanomial is built from consecutive pairs, so after the first forfeit every pair straddled two different openings and the trailing game vanished unmentioned — a sample nobody computed, printed on the one line that exists to keep a forfeited run honest, and by construction able to read `h1`. That is D-158's own failure mode moved down one line. Fixed by dropping whole pairs (`score::pairs_without_forfeits`), and the count of dropped pairs is now reported, because a conclusion drawn from a smaller sample than the run played is not the same conclusion.

**Two ADR sentences the code did not honour, both now true rather than amended.** D-159 promised the signal would be recorded and the code emitted a constant string for every signal death, which cannot tell an OOM kill from a crash — the signal number is now in the message. Two module docs claimed the handshake `id` lines were copied into the report; they were gathered and discarded, and no `engine_id` line was ever emitted, so the run's instrument was missing the candidate radius and the table size that make it re-runnable (rule 6). The engines are now handshaken once before the run to gather them, which additionally moves a mode or protocol refusal from "abandon a started run" to "refuse before any work" — exit 2 and no report, which is what the binary's own documented exit semantics always said.

**The coverage gap that mattered most.** Every end-to-end test was a self-match, where both games of a pair are the same game and every pair therefore scores alike, so no sample could ever cross a bound: the sequential stop, its pair-boundary rule, its truncation and the discarded count had no test at any level, and both worker-invariance checks would have passed unchanged with the whole stop block deleted. Closed by giving the stub a mirror-image mode — same code, largest cells instead of smallest — so two well-behaved deterministic engines win different openings and produce real pair-to-pair variation (measured over 80 openings: buckets 9 / 0 / 49 / 2 / 20, nothing degenerate, zero duplicate games). `stop_tests.rs` now runs a crossing sample at one worker and at four and compares the truncation point and the verdict block.

**Four tests that were weaker than they read.** The pair-unit equivalence test used a sample with mean exactly one half, where the evidence term vanishes on both sides, so only the drift term was ever compared — the property the `sqrt(2)` exists to give was untested; it now uses an asymmetric record (200 games at 120-80 against 100 pairs at 16/0/48/0/36, both giving 0.4567584686550011, difference exactly zero) and asserts that an unscaled pair value would visibly differ. The normalized-Elo test compared an expression against itself; it now asserts 800/ln(10) and a point estimate by value. And a stated intermediate in the reference table was wrong at the sixth significant figure — the assertions were right, but D-155's whole argument is that a reviewer recomputes the table from the stated intermediates, and that one did not reproduce. Recorded because a hand-computed table with an unreproducible intermediate is the failure mode D-155 exists to avoid, found in the very table it argues for.

**Also fixed, each cheap and each a real hole.** The stop rescored every prefix from scratch after every completed game, which is the pair count squared per game and the run's length cubed overall — some 8e9 record visits at the 1591-opening book this arena exists to play; it is now accumulated. `verdict_block` split on an unanchored substring, so an engine's verbatim refusal containing `# timing` could truncate the block the worker-invariance test compares — it is anchored at a line start. An info-line check lacked its trailing space, so a line beginning `information:` was swallowed as a report instead of forfeiting as a protocol error (rule 3). `score::verdict` carried an `exhausted` parameter whose two branches returned the same value — a parameter no test could have caught going wrong. `Channel::try_receive` and `ArenaError::has_partial_run` were dead: the second is worse than unused, because it encoded a policy about which failures keep their evidence that nothing enforced, while the actual rule is simpler and lives in one place — a failure inside the scheduler keeps the completed prefix, a refusal before it writes nothing.

**What the review checked and found correct**, recorded so it is not re-derived: every reference-table row recomputed independently and reproducing bit for bit; the `800` and not `1600` in the normalized-Elo scale; the Wald bound orientation, distinguished from a sign flip by an asymmetric error-rate case; the degenerate predicate sound at every reachable sample size, with the first spurious positive variance only beyond 1e15 observations and failing SAFE (toward refusal) when it comes; no LLR obtainable from a zero-variance sample; the seat-to-engine map correct in all four combinations; the turn cap off by exactly nothing; every path from worker count, completion order or clock into the verdict block enumerated and none found; and no swallowed error on any decision path — flips never; this line is history, and the lines it describes are the record.

D-172: The referee frames the exchange, and a line the arena did not ask for is a forfeit. Two RED-TEAM exploits, one of them the worst defect this work package produced, and both fixed here. **The unsolicited line.** The channel under the referee is a plain queue of lines and the protocol has no request identifier to match an answer to its question (D-2 pins the verb set), so a line an engine volunteers is not noise: it is read as the answer to the NEXT `go`, asked from a position the engine was never shown. Reproduced with a nine-line script engine that answers one `go` with two `bestmove` lines: the second was replayed as that engine's move two turns later, and the game finished as an ordinary `p1_win` counted into `n`, into `distinct_n`, into its pentanomial bucket and into the sample — with nothing anywhere in the report to say its move list is not what either engine intended. That is precisely "a number that is not a measurement" (CLAUDE.md rule 6), and it needs no malice: any engine build that ever emits a second `bestmove` — a buffering slip, a stray retry, a ponder racing the answer — silently corrupts every later turn of that game. The fix is a queue-emptiness check at both ends of every exchange, before the question and immediately after the `bestmove`, each asking only what has ALREADY arrived so that no wall-clock decision enters the referee (D-159). The residual limit is stated in the code rather than implied: a line arriving late enough could still be mistaken for the next answer, and no framing this protocol offers can close that; what is closed is every engine that has spoken by the time it is asked again, which is the reachable case. **The over-long line.** The read cap handed its truncated chunk over as though it were a complete line, so an engine streaming endless bytes beginning `info ` was read as an endless run of legitimate progress reports and the watchdog that fires on silence never got the chance to — measured at 2450 ms against a 400 ms `hang_timeout_ms`, with an ordinary `arena_report` written at the end rather than an abandoned one. A line over the cap is now its own outcome and forfeits by name; it is a forfeit rather than an abandonment because writing a megabyte with no newline is a deterministic wrong answer, which is the class D-159 says may be adjudicated — flips never.

D-173: `[sprt]` error rates are refused when they make a Wald boundary non-finite, and every float in a refusal or a summary is width-bounded. RED-TEAM's third exploit: `alpha > 0.0` admits subnormals, `ln((1 - beta)/alpha)` overflows, and `alpha = 1e-320` produced `bounds h0 -2.995732274 h1 inf` inside the verdict block — a non-numeric field in the machine-readable report, an H1 that can never be crossed, and therefore a silently mis-calibrated one-sided test that exited ZERO. Validation now computes the boundaries and refuses an impossible document rather than clamping it, which is what every other bound in that file does (D-18). The second half is smaller and real: `Display` on an f64 spells a subnormal out in full, so the refusal itself and the summary's bounds line ran to several hundred characters on one line; both now render in exponent form. Recorded as its own line rather than folded into D-172 because the two have different shapes — one is a protocol defect, this is a schema hole — flips never.

D-174: Records the WP-1.2b RED-TEAM round, including what it could not reach, so that unexplored surface is not later mistaken for cleared surface. Three exploits confirmed and fixed (D-172, D-173); the first was severe enough that it, not any design question, was this work package's most dangerous defect. What the round tried and the code correctly refused, which is the coverage evidence: an already-won opening (refused through `PositionSpec::replay`, so by shared rules code rather than by the arena); a genuine lattice-symmetry duplicate opening, refused by name — and worth noting, a hand-guessed "negate q" is NOT a lattice symmetry and was correctly accepted as distinct, which is a property of the group and not a miss; `bestmove 999999,999999`, which fails i16 parsing and forfeits as a bad bestmove; an openings body with no trailing newline; `hang_timeout_ms` at `u64::MAX`, which does not overflow an `Instant`; `turn_cap` and `openings_take` at their extremes, both typo-ceilinged before any arithmetic; and report-block truncation by an injected `# timing` line, which is structurally impossible because every line the arena sees came from a `read_until` on a newline, so engine text cannot contain one. The variance-cancellation attack was pushed further than REVIEW-impl took it and independently confirmed safe: all bucket scores are dyadic with denominator at most four, so a mathematically degenerate sample computes variance as exactly zero for every count below 2^53, and the first spurious non-zero appears at 9007199254740973 observations against a schema ceiling of two million — and it fails toward refusal rather than toward a tiny positive that would inflate the statistic. **NOT reached, and therefore not cleared:** a four-thousand-worker stress run with its zombie and file-descriptor accounting, and whether a panic in one worker poisons a mutex and hangs the scope; NUL bytes and invalid UTF-8 in an engine's output; `pistolok` sent twice; an engine ignoring `quit`; an engine closing stdin but not stdout; and a grandchild holding the pipe open past the direct child's exit. The first is a real gap in a way the others are less likely to be, because the concurrency surface is where a wrong verdict could still hide and it is the one surface no test in this crate exercises above four workers — flips when a second RED-TEAM pass covers the concurrency surface, which WP-1.3's first real run over the full book will motivate anyway.

D-175: A SEEDED RANDOM OPENING BOOK, and it is the PRIMARY book every SPRT runs over. OPERATOR RULING, and it supersedes nothing: `openings_v1.txt` is retained unchanged as the realism cross-check suite, and what this adds is a second book with different properties. `random_openings_v1.txt` holds 500 five-stone openings, each P1's stone at the origin and then P2's two and P1's two, sampled inside hex-distance 5 of the origin under a recorded seed and folded by the same canonical key WP-1.2a dedupes with (D-137). Three reasons it is primary. The research report's Deliverable 4 prescribes synthetic openings and the corpus book was the deviation, not this. Pairing already neutralises whatever per-opening bias any one of these carries, because both engines play both seats of every opening — so an opening being "unfair" costs a pair nothing, which is exactly the property a curated human book is *bought* for and does not need to be. And the supply of distinct games is unbounded here: the corpus book holds 1591 canonical openings and the corpus that produced it is finite, while this file's parameters can be asked for more of, which is the constraint CLAUDE.md rule 6's distinct-n reporting eventually runs into. **The stone count is a TURN-BOUNDARY count and 4 is a named refusal, not a rounding.** A turn places two stones (game rule 3), so an even count names a position no game is ever *at* — it is halfway through a turn — and `k_stones = 4` is refused as mid-turn rather than as unsupported, because those are two different complaints and only one of them would be answered by widening the set. The set is {3, 5} and the default is 5: three stones is P1's opener and P2's reply and little else. **There is NO BALANCE FILTER at this k, and the reason is arithmetic rather than a judgement.** A turn places two stones, so a mate in one TURN needs four own stones already in a six-window, and a mate in one PLY needs five; at five stones the largest holding is three, so no position in this file holds a mate in one for either side and there is nothing for a filter to remove. That arithmetic is also what keeps this file a FIXTURE under rule 7 rather than a book under rule 8, by D-151's own test: no engine judgement went into it, and D-151 says in as many words that a balance-filtered book WOULD be a book. The filter question reopens the day k grows past five, and it reopens as a rule-8 question and not only as a curation one. **Review scope**, recorded because it is narrower than the Process section's default: one fresh-context REVIEW-impl, no REVIEW-design, and no RED-TEAM. RED-TEAM was judged not required for this generator specifically — every position it emits is replayed through the same pistol-core path the arena and the engine use before it is written, so its output cannot be a position the rules refuse; and its only input is a committed config held to the same schema discipline as every other document under `configs/`, so there is no untrusted-input surface of the kind the WP-1.2b round attacked — flips if the tool ever reads a document it did not ship with, or if k grows past the arithmetic above.

D-176: `random-openings` is a THIRD binary in pistol-cli with a config document of its own, and this amends the crate map a third time (D-136 for `corpus-extract`, D-166 for `arena-stub-engine`) and D-168's document-kind list a second time. Not a mode of `corpus-extract`, and the deciding fact is that the two share no input: that tool's every path begins by reading an external corpus of human games and identifying it by SHA-256, and this one reads a committed TOML document and nothing else. They share no output and no code path either; what they share is the fixture *form*, and that is `corpus::emit`'s serialization discipline (D-147, D-148) rather than anything about a corpus. **Why this tool has a config where its sibling states that none reaches it.** `corpus-extract`'s argument is about an operator's TYPING — a fixture whose shape came from a command line is not reproducible from its own header — and it is not an argument against configuration. This document is committed, every field of it is echoed into the header of the file it produced, and a test regenerates that file from it and compares the bytes, so the four parameters are as pinned as constants would be and are visible in one document instead of spread through a source tree. It is a config rather than constants because the operator's ruling names `k_stones`, `max_radius` and `seed` as knobs, and `max_radius` in particular must be a knob that is visibly NOT `LEGAL_RADIUS`. pistol-cli grows serde, toml and serde_path_to_error to hold it; nothing on the engine's path in this crate reads TOML, and the crate already linked all three through pistol-engine, so the shipping binary gains no byte and no new transitive surface. That growth broke a recorded property and the breakage is recorded rather than quietly absorbed: `pistol_cli_manifest_names_only_core_and_engine` asserted the crate's WHOLE dependency list, and the scanner beside it said in its own doc comment that this crate has no TOML dependency. The test now asserts two things instead of one, because only one of them is a rule — the pistol-* subset is still exactly {core, engine}, which is CLAUDE.md rule 11's seam and is unmoved, and the full list is a ledger whose only job is that a fourth entry cannot arrive unremarked. That ledger claim was FALSE when it was first written and REVIEW-impl proved it so: the manifest scanner under all three shape tests turned itself off at any `[`-line it did not recognise, so a table-form `[dependencies.pistol-search]` added pistol-search to pistol-cli and left every assertion green — the rule half, not only the ledger half. The hole predates this work and the scanner backed the old assertion too, but this line is what made it load bearing, so it is closed here — and closed as a CLASS rather than a spelling, which the first fix was not. Recognising the table form left `[target.'cfg(unix)'.dependencies]` open, and a fourth spelling would have left a fifth: the scan now RECOGNISES two forms and REFUSES any other section header that names dependencies, so a manifest it cannot read is a red test rather than a green one. It fires on nothing in this workspace today, which is the point — it fires the day someone writes a spelling it does not know. dev-dependencies and build-dependencies stay excluded deliberately, because they do not ship. A guard with a door in it is worse than no guard, because it is cited. The scanner stays a scan for a reason that survives: a manifest test that parsed manifests with the parser under test would be reading the workspace through one of the things it polices. `tools/config_check.sh` learns the fourth kind, told apart by the name `random_openings_*.toml`, and the gate now says in its own header why a new kind must be named there the day it is committed: the fallback is "engine config", so an unrecognised document is not skipped, it is checked against the wrong schema and refused for every key it lacks. Also here, and smaller: the `--name value` grammar both curation binaries take moved into `pistol_cli::flags`, for the reason `count` gives for numbers — a rule with two implementations can hold two answers, and "unknown flag" is exactly the kind of message that drifts. `corpus::emit` was left where it is rather than being lifted out of the `corpus` module to serve two callers; that is one import reaching across, considered and declined as churn against a just-landed tree — flips at a third consumer.

D-177: The generation procedure, pinned here because every clause of it is a reason the file has these bytes and not others. **The PRNG is SplitMix64, reimplemented rather than depended on**, for the reason pistol-core is std-only: a fixture's bytes must not move because someone else's crate changed a constant or a default algorithm. What makes a reimplementation safe is an oracle, and this one has a published stream — the reference implementation's output from state 0 — which `random_openings_rng_tests.rs` pins it against. Every operation is `wrapping_*` on `u64`, so the stream is identical on every target and under D-127's release overflow checks. **Index sampling is by rejection**, not `% bound`: the modulus alone leans on the low residues by one part in `2^64 / bound`, invisible at a 91-cell ball and still a thumb on the scale of a book whose whole purpose is to be unbiased. At any bound this schema allows the rejection branch is unreachable — under one part in `2^57` — so it is tested at a bound where it is not, and what the test pins is the semantics: the discarded words are gone from the stream, not folded. **One word per stone.** A stone is drawn from the cells of the ball this candidate has not used yet, rather than from the whole ball with a redraw on a hit; the distribution is the same, there is no loop, and the stream's position after a candidate does not depend on how that candidate came out. Turn 1 is placed at the origin and never drawn (game rule 3). **The dedupe key is `canonical_form` over the twelve lattice symmetries** — D-137's key, which is also the key the arena's openings READER refuses a duplicate opening by, and deliberately NOT the key it counts distinct GAMES by: D-163 keys a game on `canonical_sequence` precisely because a position's canonical form would merge two different games that reached the same stones. An earlier draft of this line said the opposite and is corrected here rather than left standing, because D-163 exists to correct that exact confusion and a record that reintroduces it is worse than one that never addressed it — the code was right throughout; only this sentence was wrong — and a collision discards the WHOLE candidate and draws the next, a resample rule that is a function of the draws made so far and of nothing else. A pool that cannot supply the book refuses with the measurement in hand rather than writing a short one, because a sample size nobody chose is not a measurement (rule 6); the consecutive-collision limit that does this can only ever decide *whether* a run refuses and never what a successful run wrote, which is why it is a named constant and not a fifth config field. **Emission is generation order**, deliberately: the draws are independent, so a prefix of this file is already a sample, and sorting by anything would make a prefix a slice of whatever was sorted on — D-143's argument, in a book that gets it for free. **`max_radius` is never reconciled with `LEGAL_RADIUS`.** It has its own typo ceiling and may be set past 8, at which point a drawn stone can land beyond eight of every stone on the board; that is its own named refusal, and it is never a silent redraw, because redrawing would quietly turn `max_radius` into "somewhere inside this, mostly" and reconciling the two radii by hand is the conflation CLAUDE.md rule 2 forbids. **And the regeneration test.** D-152 found committed fixtures whose bytes the build could no longer produce, because every test read the committed file and none compared it against fresh output; that hole is closed by construction here rather than by care, since this book's only input is a committed config, so `random_openings_v1_is_what_this_build_produces` is a test and not an errand with an external corpus in it. Editing the header prose without regenerating is a red test, which is precisely what could not be seen before. Recorded from the mutation round, in a separate worktree, and corrected where REVIEW-impl found the first account of it inaccurate: removing the dedupe, the origin's reservation, the ball's boundary, the emission order, the seed, the mid-turn/unsupported ordering, the schema-version equality and `deny_unknown_fields` each turns a named test red. Removing the consecutive-collision limit does NOT — it HANGS, because the test that covers exhaustion then loops forever, so CI would stop rather than fail; the first draft of this line called it a red test and that was wrong. Giving rule 5 its own named refusal was a change the round FORCED: with the guard folded into a generic placement error, deleting it changed nothing any test could see. Two mutations survive and are named rather than left for the next reader to find, both of them in `spell` and both of the same kind — the comparison of the re-read position's key against the generated one, and the comparison of the re-read move list against the one written. Each is a belt-and-braces check with no fault this suite can inject, and the reason that is acceptable rather than dead code is that a mutation which reorders stones ACROSS turns, changing their colours, is caught by the regeneration test with both checks disabled — so neither is ever the sole detector of anything. A third survivor was NOT acceptable and was closed: an off-by-one in the rejection zone (`u64::MAX % bound` for `2^64 mod bound`) biases one index by one word in `2^64`, which no sample this tool draws could ever show, so the arithmetic is now its own function and is asserted against a 128-bit oracle over a table of bounds. That is the shape any unreachable-by-measurement invariant should take here.

D-178: Real arena runs cap `n_workers` at 4 until a dedicated concurrency pass covers the surface D-174 lists as not reached, and the cap lives in WP-1.3's pre-registration rather than in code. In the pre-registration because it is an experimental protocol and not a property of the runner: the config field is a knob whose safe range is a fact about how well the surface has been tested, which changes when the testing changes, and a ceiling compiled into `validate.rs` would have to be edited by ADR every time it moved. Four because that is the highest worker count any test in the crate exercises — `stop_tests.rs` compares a crossing sample at one worker and at four — so it is the boundary of what has actually been shown, not a number chosen for comfort. What is uncovered above it is D-174's own list: zombie and file-descriptor accounting at scale, and whether a panic in one worker poisons a mutex and hangs the scope. A wrong verdict is the failure mode this guards, which is why it is a hard cap on real runs and not advice — flips when a second RED-TEAM pass covers the concurrency surface, which is the same event D-174 names.

D-179: WP-1.3's pre-registration states the verdict level — pair-level GSPRT per D-154, or game-level — IN WRITING before the first game of the first experiment, and no run may change it afterwards. The Process section already forbids post-hoc threshold moves and this line makes its application to D-154 explicit, because D-154 is exactly the kind of choice that invites a quiet re-reading: it demotes the game-level LLR to a reported diagnostic, and a report that carries BOTH numbers is a report where reading the other one after the fact requires no code change, no config change and no visible act. Naming the level in the pre-registration turns that from an unlogged choice into a contradiction of a document — flips never; the rule is the existing Process rule and this line is its application.

D-180: The desync residual WP-1.2b's RED-TEAM left open is ACCEPTED while both ends of every arena match are pistol CLI builds. D-172 closed every engine that has finished speaking by the time it is asked again, and stated the limit it cannot close: the protocol has no request identifier (D-2 pins the verb set), so a line arriving late enough could still be read as the answer to the next question. What makes that acceptable is not that it is unlikely, it is that both ends are the same synchronous-by-design program (D-2): a pistol build answers exactly one line per question and emits nothing unbidden, so the late line has no source. The ruling flips the day any non-pistol engine connects to the arena — which is separately forbidden, and this line exists so the boundary is findable when someone proposes lifting that — at which point the fix is a protocol-level request identifier echoed in the answer, not a wider queue check.

D-181: Records the random-openings REVIEW-impl round, because two of its findings were sentences in D-176 and D-177 that the code did not support, one was a guard that had a door in it, and a reader of those two lines should know which clauses were corrected and why. Fresh context, against a named revision, in a separate worktree. **The guard with a door.** The manifest scanner behind all three workspace-shape tests set `inside = (line == "[dependencies]")` on every `[`-line, so the TABLE form of a dependency turned the scan off and its body was skipped: `[dependencies.pistol-search]` added pistol-search to pistol-cli, cargo resolved and built against it, and every assertion stayed green — including the one this session had just relabelled as CLAUDE.md rule 11's seam. Reproduced in both spellings, fixed by recognising the table form, and both are red now. The hole predates this work; what is new is D-176 citing it. **The record that reintroduced a corrected confusion.** D-177's first draft said the generator's dedupe key "is also the key the arena counts distinct games by (D-163)" — and D-163 says the opposite in as many words, because it is the line that corrected exactly this in the arena's own design. The code was right throughout: the generator folds openings by `canonical_form`, which is what WP-1.2a does and what the arena's openings READER does, while the arena's distinct-GAME key is `canonical_sequence`. Corrected in the ADR and in the test comment that had copied it. **A validator on one of two doors.** `generate` is public, every field of the config is public, and validation lived only in `parse`, so a struct literal reached the generator with any stone count and any radius — including k = 7, where a side holds four stones and D-175's no-balance-filter arithmetic stops holding. `generate` now revalidates, and the `unwrap_or(i16::MAX)` that would have silently clamped an absurd radius is an `unreachable!` naming the invariant instead. **Coverage the round added.** The binary was executed by no test and no gate — `corpus-extract` is driven through `CARGO_BIN_EXE_` at seven sites and this one at none — so its exit codes, its refusals, its `--help`, and the staged-then-renamed write that must not replace a good book with a partial one are now pinned by a CLI test file, including a two-PROCESS byte-identity check matching `tools/determinism.sh`'s shape. A second pass over that file closed the last branch nothing reached, which was the cleanup after a rename that failed: a directory standing at the book's own name lets the staged write succeed and the rename fail, which is the only way to reach it, and what the test asserts is that the staged file does not outlive the run — a half-finished output left beside a name that does not exist yet being the thing the staging exists to prevent. The write-failure paths are chosen to be root-safe: a permission bit stops meaning anything when tests run as root, and a directory standing where a file must go does not. Three of the seven operator-named tests read only the committed fixture, which its own digest already pins, so their comments claimed of the generator what they asserted of the file: the radius test now generates at two further radii and catches the ball's boundary mutation, which it previously did not. And the rejection-zone arithmetic is now checked against a 128-bit oracle (D-177). **What the round checked and found correct**, recorded so it is not re-derived: the sampler's uniformity, by chi-squared over a million draws and by an independent reimplementation in another language that reproduced the committed book's opening lines from the seed alone; the SplitMix64 stream against the published reference; the rejection-zone identity proved at bound 1, 2, `2^63 + 1` and `u64::MAX`; the colour assignment re-derived over all 500 lines; no position won, illegal, mid-turn or duplicated; no nondeterminism on any path into the bytes; every failure path leaving no partial or stale file; the mate-in-one arithmetic including that four-in-a-six-window is the right threshold for a turn and five for a ply; the `flags` extraction character-identical in every refusal message; and the arena's own loader reading the finished book, 500 openings with the in-band digest verified — which is a fact worth having written down, because nothing in the tree asserts it (see below) — flips never; this line is history, and the lines it describes are the record.

D-182: NOTHING IN THE TREE PINS THAT THE ARENA CAN LOAD THE BOOK D-175 CALLS PRIMARY, and that gap is recorded rather than closed, because closing it means touching pistol-arena and this session's scope forbids it. The two serializations are written in different crates — pistol-cli emits the file, pistol-arena's `openings::load` reads it — and every property the reader requires is asserted today only on the WRITER's side: the `# body_sha256` header and body offset, a payload of position tails and no stray `#` line, a uniform turn count, and no two openings equal up to a lattice symmetry. That is not nothing, and it is not the same as the reader having read it. REVIEW-impl loaded the committed book through `pistol_arena::openings::load` by hand and it succeeded — 500 openings, in-band digest verified, no duplicate refusal — so the compatibility is a fact as of this revision and not a hope; what is missing is anything that would notice the day one side's serialization moves. The fix is one test in pistol-arena and belongs with WP-1.3, which is where the arena is next opened and where the pre-registration must name the book anyway (D-178, D-179) — flips the moment WP-1.3 starts, and this line is the reminder that it is owed.
