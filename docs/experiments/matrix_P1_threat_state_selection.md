# MATRIX P1 — SELECTION RECORD

**Selected: O-E — per-axis line bitboards with a logged undo**, as
`matrix_P1_threat_state.md` revision 3 recommends and as three fresh-context
DECISION-RED-TEAM rounds left standing (`matrix_P1_threat_state_REDTEAM.md`,
`…_round2.md`, `…_round3.md`). In every round the verdict's first clause was
*"the recommendation SURVIVES"*; what failed each time was the RECORD — an
artifact that did not hold what it was cited for, a field missing D-254's own
alternative, a grep whose stated scope could not produce its table, a
committed seat called uncommitted, an estimate called a ceiling, and a
pre-registration block that read a keyed fixture as bare — and each was
corrected and re-executed (round 2 executed every round-1 remedy; round 3
every round-2 one). The numbers never moved: every whole-engine ratio, every
counter, every digest and every line count reproduced under commands the
reviewers chose, most of them from instruments the reviewers built.

**Not selected, and why, in one line each.** O-A (lazy at the seam): measured
2.8 / 2.1 %, at the estimate the unqueried share gives it; a different
mechanism in a different crate, which the design excludes and rule 5 would
bench on its own. O-B: bounded by O-A, dearer. O-C: measured zero (no flush
ever had two stones pending). O-D1: 1.6 %. O-D2: 1.128 / 1.113, superseded by
O-E on the same log. O-E0: 1.190 / 1.188, the same store with an eager undo —
one row up for a reader who prices the LIFO contract higher than the matrix
does. O-F: WP-1.9's measured prior and the lattice-edge class. O-G: a
cross-crate seam, its own package if ever. O-I: D-254's own 4.0–4.8 % of the
state's cost, stacked ≈ 1.13–1.15, eleven points under O-E. O-H: 35.67 % stays.

**What the selection binds the design to** (`p1_design.md`): the LIFO-or-panic
`undo` and the hand-written equality, each pinned by a named test; the three
chunk-boundary tests and the lattice-edge test, which the store has no
falsifier for until they exist; `table.rs` split so the store is its own file
under rule 9's cap; the `reset_to` replacement stated as load-bearing; no
claim about the solver-armed seat's throughput, where the same two binaries
measured about two per cent apart early and indistinguishable late.

**The strongest surviving attack, carried verbatim from round 3** (CLAUDE.md's
OPTION MATRIX clause):

> O-E's 24–26 % is the instrument seat's number and only that seat's: at the
> committed, CI-gated solver-on seat — the one whose caller drives the
> LIFO-or-panic contract hardest — the same two binaries are about two per
> cent apart early and indistinguishable late, measured once, exploratory, on
> one hand-rolled instrument, so the gain the package banks is a property of
> one node mix and the narrowing is bought, at the solver's seat, for nothing
> measurable; the one check the package offers at that seat, the identity
> leg's third seat, cannot run as registered, so byte-identity at the solver's
> seat is at this revision a promise and not an instrument; the premise that
> licensed the matrix's shape is loose at the edge, since O-A's measured gain
> sits 0.3–0.9 points above the per-band estimate the unqueried share gives it
> and the discrepancy is recorded rather than explained; the one alternative
> D-254 called faster is still rejected on a figure from another instrument at
> another revision, unprototyped, its stacked estimate stated for one band; and
> the chunk-boundary and lattice-edge behaviour of the store that wins has no
> falsifier until the design's I5 and I6 exist.

Of its five limbs: the identity leg's block is rewritten on the determinism
gate's own fixture parsing and is executed before the prereg's gate opens; the
stacked estimate is now stated per band; I5 and I6 are the design's and exist
on the implementation branch; the O-A discrepancy stays recorded; and the
first limb stands as the measurement's limit — the package claims its gain at
the instrument seat and nothing at the solver's.
