# Repository audit — 2026-09 (read-only findings ledger under D-569(a))

**Revision audited: `d83ac01`**, in a detached worktree at
`/home/tom/pistol-runs/audit-2026-09` (`git worktree add --detach`), its own
`target/`, nothing exported. Binary built there: `cargo build --release
--locked -p pistol-cli`, `target/release/pistol` sha256
`7a7a2347a7af56a103d0fa512bb6838361d258e3a146ac71ac9903c2c6ec6a6e`. Every
measurement below ran after the overnight arc's perf guard had written `done`
(`artifacts/wp20b_perf_guard_v3.txt`) and with no other engine process alive.
Every `file:line` is at `d83ac01`. No fix is proposed beyond the package column;
a finding without a reproducer, receipt or citation was deleted before landing.

**Methods, by class.** BUG: read the line-protocol path end to end
(`crates/pistol-cli/src/{serve,protocol,budget_token}.rs`,
`crates/pistol-engine/src/{instance,position,position_*_token}.rs`) and every
`panic!/expect/unreachable!` in non-test `src/` (196 sites, `grep`), then drove
the shipped binary with adversarial but LEGAL inputs; a probe that refused
cleanly is not listed. PERF: `perf record -F 2000` on the shipped binary at the
committed instrument seat (`configs/instrument_v0.toml`), receipts quoted.
REDUNDANCY: `grep` for the same grammar or rule in two homes, both cited.
STYLE: mechanical counts over non-test `src/` (commands in the row), sampled,
not enumerated. DEBT: pragmas, warnings, consumed-nowhere config keys, and
guards whose message string appears in no test (D-553's class).

## Ledger

Severity: BUG / PERF / REDUNDANCY / STYLE / DEBT. Package shape:
byte-identity-guarded (search output unchanged, gate = the golden transcripts)
/ bench-guarded (rule 5) / SPRT-required (rule 6) / docs-only.

| id | crate/file:line | class | evidence | severity | package shape |
|---|---|---|---|---|---|
| A-01 | `crates/pistol-search/src/search.rs:423` (`abortable = depth_turns > 1 \|\| fallback.is_some()`), `pvs.rs:105-110`, `staged.rs:222-235` (uncapped quiet-ball safety net), `pvs.rs:364-365` (root turn exempt from the cap) | BUG — a node budget is not a bound on a legal position | Reproducer (scripts in the receipt): a legal 257-stone position of stones spaced 8 apart along one axis, `go nodes 500` at the committed instrument seat → `info totals depth_turns 1 nodes 28661760 time 43895`; a legal 2 049-stone spacing-2 grid, `go nodes 2000` → `nodes 10505121 time 32524`. The un-abortable first iteration is O(\|quiet ball\|²) at the root turn (ESTIMATED ≈ 4 900² plies for 257 stones at `quiet_radius = 2`; the MEASURED 28.66 M nodes is consistent with it) and `Stop::Nodes` promises `n.next_multiple_of(1024)` (`stop.rs:10-13`); `stop.rs:28-35` documents the class at "five of twenty late-game positions … at 1 000 nodes" — the magnitude here is 57 000×. Play mode is protected: same position, `configs/play_staged_v0.toml`, `go movetime 500` → wall 0.52 s, `Provenance::PartialRoot` (fallback works). Rule 6's equal-per-side-compute premise does not hold on such positions. | BUG | SPRT-required (any remedy — an abortable first iteration, a root-turn width bound — moves node-budgeted answers, so the golden transcripts cannot guard it) |
| A-02 | `crates/pistol-eval/src/handcrafted.rs` (`Eval::delta`), called from `crates/pistol-search/src/staged.rs:287-295` (`delta_rank`) and `ordering.rs:44-56` | PERF — the per-node hotspot is the delta ranking of an UNCAPPED candidate set | Receipt: `perf record -F 2000 -- target/release/pistol --config configs/instrument_v0.toml` on `bench_positions_v1.txt` line 2 (15 stones), `go nodes 400000`; `perf report --no-children --percent-limit 1.5` (2 105 samples): `31.77% HandcraftedV0::delta`, `13.45% ThreatState::touch`, `9.55% WindowTable::masks`, `7.98% WindowTable::set`, `6.64% HandcraftedV0::undo`, `6.29% HandcraftedV0::apply`, `5.14% WindowSets::transition`, `2.60% win::extents`, `2.55% Coord sort`. D-263 registered the COVER arithmetic as WP-1.5b's hotspot; at this seat `blocking_covers` is below the 1.5 % floor and the delta rank is the top entry — a finding against the registered expectation. | PERF | bench-guarded for a cheaper delta; SPRT-required for any width cap (the cap is C-2 of `search_gap_2026-09.md`) |
| A-03 | `crates/pistol-solver/src/state.rs` (`ThreatState::touch`), `table.rs` (`WindowTable::masks`, `::set`), `sets.rs` (`WindowSets::transition`) | PERF — threat-state maintenance per placed stone is 36 % of the committed seat's wall | Same receipt as A-02: `touch 13.45 + masks 9.55 + set 7.98 + transition 5.14 = 36.12 %`, paid on every `Position::place`/`undo` (`crates/pistol-search/src/position.rs:108-117,126-144`) whether or not the node reads a threat query. | PERF | bench-guarded, output byte-identical by construction (the state is a cache of the board) |
| A-04 | `crates/pistol-core/src/board.rs:152-159` (`in_legal_region`: `.any(\|stone\| distance <= LEGAL_RADIUS)`), called per candidate cell from `crates/pistol-search/src/candidates.rs:56` (`filter(\|cell\| board.is_legal_placement(cell))`) | PERF — rule-5 legality is O(stones) per cell and IS paid per candidate, contrary to `board.rs:148-151`'s own claim that "candidate generation … never pays this per candidate" | Receipt: `perf record` on the 257-stone chain, `go nodes 500`: `18.18% ThreatState::touch`, `12.13% WindowTable::masks`, `10.32% WindowTable::set`, `10.30% Board::check_placement`, `9.85% Board::is_legal_placement`, `10.07% eval apply`, `9.17% eval undo`, `6.65% delta`. Growth: spacing-2 grid, `go nodes 2000` — 257 stones 0.25 s, 1 025 stones 2.82 s, 2 049 stones 33.2 s, 4 097 stones 58.7 s. The doc comment is also a D-424-class claim the code does not make. | PERF | bench-guarded (a bounding-box or per-stone-ball region test), byte-identical outputs required; the comment is docs-only |
| A-05 | `crates/pistol-search/src/pvs.rs:636-677` vs `search.rs:757-800` (census columns computed twice, field for field); `pvs.rs:753-769` vs `search.rs:803-819` (row push twice) | REDUNDANCY — one census row, two constructions | Both sites read `hot_windows`, `win_in_one_ply_windows`, `live_windows_at_count(Three)`, `blocking_covers` in the same order into the same `TriggerColumns`; the root copy exists for a borrow reason `search.rs:753-756` states. A column added to one and not the other splits the census silently. | REDUNDANCY | byte-identity-guarded refactor (census rows compared by digest, `census_protocol_tests.rs`) |
| A-06 | `crates/pistol-search/src/search.rs:659-664` vs `search.rs:713-727` | REDUNDANCY — the `WinWitness::OnePly` partner degeneration is written twice, with different failure behaviour (`?` vs `expect`) | `proof_first_move` returns `None` and `proof_line` panics on the same impossible board. | REDUNDANCY | byte-identity-guarded |
| A-07 | `crates/pistol-search/src/ordering.rs:66-72` vs `staged.rs:86-96` (table-move rotate); `ordering.rs:44-56` vs `staged.rs:287-295` vs `quiescence.rs:436-447` (score-then-stable-sort / argmax by delta) | REDUNDANCY — the same ordering primitive in three homes | Three implementations of "rank by `delta`, coordinate tie-break"; the tie-break rule D-5/D-7 pin is restated at each. | REDUNDANCY | byte-identity-guarded |
| A-08 | `crates/pistol-arena/src/exchange.rs:193`; `tools/sealbot/matchserver/src/pistol_client.rs:41,241`; `tools/cold_label_check.py:182`; `tools/baseline_snapshot.sh:634`; `tools/bench_block.sh:264`; `tools/determinism.sh:170`; `tools/movetime_check.sh:125`; `artifacts/wp20b_perf_guard.sh` (uncommitted, awk `/^info totals /`) | REDUNDANCY — the `info totals` grammar is parsed in eight homes; D-80's marker discipline lives in `crates/pistol-cli/src/report.rs:39` and every consumer re-derives it | Sites listed by `grep`; each keys on a literal (`' totals '`, `"info totals "`, `^info totals`). D-542 already priced "a second totals reader" as a kill condition for one design row. | REDUNDANCY | docs-only (a register of consumers in `tools/SHELL_CHECKLIST.md`); a shared reader is a tools/ package of its own |
| A-09 | `crates/pistol-cli/src/sha256.rs`; `crates/pistol-core/tests/common/sha256.rs`; `crates/pistol-solver/tests/common/sha256.rs` | REDUNDANCY — three SHA-256 implementations (two deliberate, D-37: no dev-dependencies in the test trees) | `grep -l 6a09e667`. | REDUNDANCY | docs-only (accepted; recorded so a fourth is not added) |
| A-10 | `crates/pistol-search/src/params.rs:117-127` ↔ `crates/pistol-engine/src/config.rs` (`QTriggers`, `CandidatePolicy` mirrored, mapped at `instance.rs:224-263`); tier-T `{2,3}` validated at `validate.rs` and again at `search.rs:135-143`; radius-fits-i16 refused at `search.rs:114-123` and again at `candidates.rs:72-79`; `search.rs:35` `MAX_Q_EXTENSION_PLIES` vs the engine's `MAX_Q_DEPTH_TURNS` | REDUNDANCY — accepted belt-and-braces duplicates, each with its ADR (D-396, D-353) | Cited at the sites; no drift found between the pairs at `d83ac01`. | REDUNDANCY | docs-only (none; listed so a reviewer does not re-discover them) |
| A-11 | `crates/pistol-search/src/params.rs:1-19` | STYLE — `//!` module doc in an ordinary (non-root) module, banned by CLAUDE.md | `grep -l '^//!' crates/*/src/**` → the only non-root hit besides a `bin/` root (`solver-cost.rs`, permitted). | STYLE | byte-identity-guarded (comment-only) |
| A-12 | `crates/pistol-solver/src/lib.rs` (105 `//!` lines), `pistol-core` 59, `pistol-arena` 45, `pistol-eval` 41, `pistol-cli` 33, `pistol-engine` 26, `pistol-search` 22 | STYLE — crate-root `//!` "at most a few lines" | `grep -c '^//!' crates/*/src/lib.rs`. | STYLE | byte-identity-guarded (comment-only) |
| A-13 | 16 blocks of ≥ 8 consecutive `//` lines in non-test `src/`: longest `pvs.rs:350` (13), `pvs.rs:227` (11), `dfpn.rs:534` (11), `conclusion.rs:135` (9), `validate.rs:196` (9), `pvs.rs:783` (9) | STYLE — "if a comment needs a paragraph, the code or the design doc is the wrong shape" | awk count over `crates/*/src`; a sample of 20 `//` comments in `pvs.rs`, `search.rs`, `dfpn.rs` found NO narration (every one states a why) — the violation class present is LENGTH, not what-not-why. | STYLE | byte-identity-guarded (comment-only), or docs-only where the text belongs in a design doc |
| A-14 | ≈ 70 `pub fn … -> Result` without a `# Errors` section (awk over non-test `src/`; approximate); e.g. `crates/pistol-arena/src/channel.rs:56`, `config.rs:197`, `handshake.rs:30`, `identity.rs:25` | STYLE — public docs "with `# Errors` / `# Panics` sections where a caller would reasonably handle them" | count + samples. | STYLE | byte-identity-guarded (comment-only) |
| A-15 | 32 trailing inline comments in non-test `src/`; 15 are `crates/pistol-solver/src/zone.rs:245-266`, one is `crates/pistol-search/src/lib.rs:31` (`pub mod search; // …`) | STYLE — "line comments, own line" | `grep -E '[^ ]\s+// [A-Za-z]'`. | STYLE | byte-identity-guarded (comment-only) |
| A-16 | `crates/pistol-search/src/position.rs:73-81` (`/// The game.` on `state()`, `/// The stones.` on `board()`) | STYLE — docs that restate the name | narrow pattern `/// The <word>.`, 2 hits; sampled. | STYLE | byte-identity-guarded (comment-only) |
| A-17 | `crates/pistol-engine/src/validate.rs:98-125` (validated), `crates/pistol-cli/src/bin/pistol.rs:159` (advertised in the handshake), `crates/pistol-engine/src/instance.rs:235-236` (`quiet_top_k: _, widen_schedule: _` — consumed nowhere); doctrine at `instance.rs:135-138` ("a config key that no code reads is an instruction that silently did nothing") | DEBT — two schema keys that no code path reads, one of them printed as PROVENANCE | Reproducer: `configs/instrument_v0.toml` vs a copy with `quiet_top_k = 128`, `widen_schedule = [256]`, same position, `go nodes 50000`: `diff` shows only the `id config` and `id candidate_policy … quiet_top_k` lines differ; every `info`/`bestmove` line is byte-identical (`nodes 50176`, `pv -2,0/-2,2 -4,0/3,2`). Two runs a transcript says are different instruments are the same search. | DEBT | byte-identity-guarded (drop the two keys from the identity line and the schema, or land WP-1.5c which consumes them) + handshake golden refresh; the choice is an ADR |
| A-18 | `crates/pistol-search/src/pvs.rs:606` (`#[allow(clippy::empty_line_after_doc_comments)]`) over `pvs.rs:591-600` | DEBT — a pragma hiding an orphaned doc block: the `should_stop` doc is detached from `should_stop` (`pvs.rs:771-773` has its own) and attached to nothing | the only `#[allow]` in non-test `src/` (`grep`). | DEBT | byte-identity-guarded (comment-only) |
| A-19 | `crates/pistol-solver/src/policy.rs:1` (`use pistol_core::{…, generate_turns}` unused) | DEBT — a rustc warning survives CI: gate 4 is `-D clippy::all` (`tools/ci.sh:86-87`), which does not deny rustc lints | `cargo build --release --locked -p pistol-cli` → `warning: unused import: generate_turns … policy.rs:1:44`. | DEBT | byte-identity-guarded; the gate question (`-D warnings`) is a tools/ item |
| A-20 | `crates/pistol-search/src/tt/entry.rs:64-66,83,120` (`static_eval` packed into every entry, "kept for the ordering and pruning decisions Stage 1 adds") | DEBT — 2 of 24 bytes per entry written and never read; Stage 1 is closed (D-503) without a reader | `grep -rn static_eval crates/pistol-search/src` → writes at `pvs.rs:525`, `quiescence.rs:128`; no read. | DEBT | bench-guarded (dropping it changes `ENTRY_BYTES` and table density, so node counts move) — or docs-only if Stage-2 pruning (C-5) will read it |
| A-21 | `crates/pistol-search/src/search.rs:114-123` (radius past `i16`), `search.rs:151-161` (`q_depth_turns` ceiling), `quiescence.rs:418-423` (`NO_COMPLETION_STONE`), `search.rs:719-722`, `search.rs:833-834` | DEBT — guards whose refusal text appears in no test (D-553's call-site class; here not even the function is pinned) | `grep -rl` for each message across `crates/*/tests` and `src` tests → only the defining file. | DEBT | byte-identity-guarded (tests only) |
| A-22 | `docs/research/sealbot_deep_dive.md` (78 entries, binding rule at its head) | DEBT — 0 of 78 entries VERIFIED or REFUTED at `d83ac01`, under a rule that no entry may drive a change until verified | `grep -c "STATUS: VERIFIED"` → 0. | DEBT | docs-only |
| A-23 | `docs/ROADMAP.md:389-404` (WP-1.10: five undriven gate scripts, sixteen `command -v` sites, seven un-preflighted `mktemp -d` scripts) | DEBT — known and licensed-not-scheduled; listed so this ledger is complete, not re-derived | as cited. | DEBT | docs-only (pointer) |

## Receipt

Reproducer scripts (scratchpad, not committed): `repro/edge_position.py`
(spaced-8 chain), `repro/compact_position.py` (spacing-2 grid),
`repro/time_positions.py`, `repro/protocol_probes.sh`; perf data
`pistol.perf.data`, `edge.perf.data`. Probes that refused cleanly and are
therefore NOT findings: `go nodes 0`, `go depth_turns 0`, `go depth_turns 65`,
a coordinate at 32 768, a `go` on a half-played turn.

`git status --porcelain` in the audit worktree at the end:
`?? docs/audit/` — the three deliverables and nothing else. The live tree at
`/home/tom/Projects/HeXO-AlphaBeta` was not touched (its own uncommitted arc
files are the operator's, and are as `git status` listed them at session start).
