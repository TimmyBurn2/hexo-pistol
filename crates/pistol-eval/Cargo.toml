[package]
name = "pistol-eval"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# The lattice, the three axes and the win length: rules truth comes from one
# place and is never restated here (CLAUDE.md rule 2).
pistol-core = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
# Recovers the exact key path of a rejected field, so every weights failure can
# name the key that caused it (docs/decisions.md D-24).
serde_path_to_error = { workspace = true }
