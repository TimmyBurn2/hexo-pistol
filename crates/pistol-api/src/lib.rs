//! `pistol-api` — RESERVED, and empty by rule.
//!
//! It stays empty until the API layer is specified (CLAUDE.md rule 11). The
//! `Engine` trait in `pistol-engine` and the line protocol in `pistol-cli` are
//! the contract that layer will adapt; a transport dependency anywhere else in
//! the workspace is a breach. Do not add code here without a spec.
