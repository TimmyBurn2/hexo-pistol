//! `pistol-api` — RESERVED, and empty by rule.
//!
//! Reserved purpose: the future API layer. It stays empty until the API is
//! specified (CLAUDE.md rule 11). The `Engine` trait in `pistol-engine` and the
//! line protocol in `pistol-cli` are the contract that layer will adapt;
//! adding a transport dependency anywhere else in the workspace is a breach.
//!
//! This crate is intentionally empty. Do not add code here without a spec.
